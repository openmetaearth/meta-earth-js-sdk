// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/gravity/v1/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Any } from '../../../google/protobuf/any'

export const protobufPackage = 'metaearth.gravity.v1'

/**
 * ClaimType is the cosmos type of an event from the counterpart chain that can
 * be handled
 */
export enum ClaimType {
  CLAIM_TYPE_UNSPECIFIED = 0,
  CLAIM_TYPE_SEND_TO_ME = 1,
  CLAIM_TYPE_SEND_TO_EXTERNAL = 2,
  CLAIM_TYPE_BRIDGE_TOKEN = 3,
  CLAIM_TYPE_RELAYER_SET_UPDATED = 4,
  UNRECOGNIZED = -1,
}

export function claimTypeFromJSON(object: any): ClaimType {
  switch (object) {
    case 0:
    case 'CLAIM_TYPE_UNSPECIFIED':
      return ClaimType.CLAIM_TYPE_UNSPECIFIED
    case 1:
    case 'CLAIM_TYPE_SEND_TO_ME':
      return ClaimType.CLAIM_TYPE_SEND_TO_ME
    case 2:
    case 'CLAIM_TYPE_SEND_TO_EXTERNAL':
      return ClaimType.CLAIM_TYPE_SEND_TO_EXTERNAL
    case 3:
    case 'CLAIM_TYPE_BRIDGE_TOKEN':
      return ClaimType.CLAIM_TYPE_BRIDGE_TOKEN
    case 4:
    case 'CLAIM_TYPE_RELAYER_SET_UPDATED':
      return ClaimType.CLAIM_TYPE_RELAYER_SET_UPDATED
    case -1:
    case 'UNRECOGNIZED':
    default:
      return ClaimType.UNRECOGNIZED
  }
}

export function claimTypeToJSON(object: ClaimType): string {
  switch (object) {
    case ClaimType.CLAIM_TYPE_UNSPECIFIED:
      return 'CLAIM_TYPE_UNSPECIFIED'
    case ClaimType.CLAIM_TYPE_SEND_TO_ME:
      return 'CLAIM_TYPE_SEND_TO_ME'
    case ClaimType.CLAIM_TYPE_SEND_TO_EXTERNAL:
      return 'CLAIM_TYPE_SEND_TO_EXTERNAL'
    case ClaimType.CLAIM_TYPE_BRIDGE_TOKEN:
      return 'CLAIM_TYPE_BRIDGE_TOKEN'
    case ClaimType.CLAIM_TYPE_RELAYER_SET_UPDATED:
      return 'CLAIM_TYPE_RELAYER_SET_UPDATED'
    case ClaimType.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED'
  }
}

export interface ProposalRelayer {
  relayers: string[]
}

export interface Relayer {
  relayerAddress: string
  externalAddress: string
  delegateAmount: string
  /** start relayer height */
  startHeight: number
  online: boolean
  slashTimes: number
}

/** BridgeValidator represents a validator's external address and its power */
export interface BridgeValidator {
  power: number
  externalAddress: string
}

/**
 * RelayerSet is the external Chain Bridge Multsig Set, each gravity validator
 * also maintains an external key to sign messages, these are used to check
 * signatures on external because of the significant gas savings
 */
export interface RelayerSet {
  nonce: number
  members: BridgeValidator[]
  height: number
}

/**
 * LastObservedBlockHeight stores the last observed
 * external block height along with the our block height that
 * it was observed at. These two numbers can be used to project
 * outward and always produce batches with timeouts in the future
 * even if no Ethereum block height has been relayed for a long time
 */
export interface LastObservedBlockHeight {
  externalBlockHeight: number
  blockHeight: number
}

/** BridgeToken */
export interface BridgeToken {
  contractAddress: string
  denom: string
  name: string
  symbol: string
  decimal: number
  supply: string
}

/**
 * Attestation is an aggregate of `claims` that eventually becomes `observed` by
 * all bridger set
 * EVENT_NONCE:
 * EventNonce a nonce provided by the gravity contract that is unique per event
 * fired These event nonces must be relayed in order. This is a correctness
 * issue, if relaying out of order transaction replay attacks become possible
 * OBSERVED:
 * Observed indicates that >67% of validators have attested to the event,
 * and that the event should be executed by the gravity state machine
 *
 * The actual content of the claims is passed in with the transaction making the
 * claim and then passed through the call stack alongside the attestation while
 * it is processed the key in which the attestation is stored is keyed on the
 * exact details of the claim but there is no reason to store those exact
 * details becuause the next message sender will kindly provide you with them.
 */
export interface Attestation {
  observed: boolean
  votes: string[]
  height: number
  claim: Any | undefined
}

/** OutgoingTxBatch represents a batch of transactions going from gravity to ETH */
export interface OutgoingTxBatch {
  batchNonce: number
  batchTimeout: number
  transactions: OutgoingTransferTx[]
  tokenContract: string
  block: number
  feeReceive: string
}

/** OutgoingTransferTx represents an individual send from gravity to ETH */
export interface OutgoingTransferTx {
  id: number
  sender: string
  destAddress: string
  token: ERC20Token | undefined
  fee: ERC20Token | undefined
}

/** ERC20Token unique identifier for an Ethereum ERC20 token. */
export interface ERC20Token {
  contract: string
  amount: string
}

/** IDSet represents a set of IDs */
export interface IDSet {
  ids: number[]
}

export interface BatchFees {
  tokenContract: string
  totalFees: string
  totalTxs: number
  totalAmount: string
}

export interface MinBatchFee {
  tokenContract: string
  baseFee: string
}

/**
 * relayer_set_update_power_change_percent
 *
 * If power change between validators of CurrentRelayerSet and latest relayer
 * set request is > 10%
 */
export interface Params {
  gravityId: string
  averageBlockTime: number
  externalBatchTimeout: number
  averageExternalBlockTime: number
  signedWindow: number
  slashFraction: string
  relayerSetUpdatePowerChangePercent: string
  maxRelayers: number
  minDelegate: string
  maxDelegate: string
  maxSlashTimes: number
  maxSendToExternalUsdAmount: string
}

function createBaseProposalRelayer(): ProposalRelayer {
  return { relayers: [] }
}

export const ProposalRelayer: MessageFns<ProposalRelayer> = {
  encode(message: ProposalRelayer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.relayers) {
      writer.uint32(10).string(v!)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProposalRelayer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseProposalRelayer()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.relayers.push(reader.string())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): ProposalRelayer {
    return {
      relayers: globalThis.Array.isArray(object?.relayers)
        ? object.relayers.map((e: any) => globalThis.String(e))
        : [],
    }
  },

  toJSON(message: ProposalRelayer): unknown {
    const obj: any = {}
    if (message.relayers?.length) {
      obj.relayers = message.relayers
    }
    return obj
  },

  create<I extends Exact<DeepPartial<ProposalRelayer>, I>>(base?: I): ProposalRelayer {
    return ProposalRelayer.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<ProposalRelayer>, I>>(object: I): ProposalRelayer {
    const message = createBaseProposalRelayer()
    message.relayers = object.relayers?.map((e) => e) || []
    return message
  },
}

function createBaseRelayer(): Relayer {
  return {
    relayerAddress: '',
    externalAddress: '',
    delegateAmount: '',
    startHeight: 0,
    online: false,
    slashTimes: 0,
  }
}

export const Relayer: MessageFns<Relayer> = {
  encode(message: Relayer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayerAddress !== '') {
      writer.uint32(10).string(message.relayerAddress)
    }
    if (message.externalAddress !== '') {
      writer.uint32(18).string(message.externalAddress)
    }
    if (message.delegateAmount !== '') {
      writer.uint32(26).string(message.delegateAmount)
    }
    if (message.startHeight !== 0) {
      writer.uint32(32).int64(message.startHeight)
    }
    if (message.online !== false) {
      writer.uint32(40).bool(message.online)
    }
    if (message.slashTimes !== 0) {
      writer.uint32(48).int64(message.slashTimes)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Relayer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRelayer()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.relayerAddress = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.externalAddress = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.delegateAmount = reader.string()
          continue
        }
        case 4: {
          if (tag !== 32) {
            break
          }

          message.startHeight = longToNumber(reader.int64())
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.online = reader.bool()
          continue
        }
        case 6: {
          if (tag !== 48) {
            break
          }

          message.slashTimes = longToNumber(reader.int64())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): Relayer {
    return {
      relayerAddress: isSet(object.relayerAddress)
        ? globalThis.String(object.relayerAddress)
        : isSet(object.relayer_address)
          ? globalThis.String(object.relayer_address)
          : '',
      externalAddress: isSet(object.externalAddress)
        ? globalThis.String(object.externalAddress)
        : isSet(object.external_address)
          ? globalThis.String(object.external_address)
          : '',
      delegateAmount: isSet(object.delegateAmount)
        ? globalThis.String(object.delegateAmount)
        : isSet(object.delegate_amount)
          ? globalThis.String(object.delegate_amount)
          : '',
      startHeight: isSet(object.startHeight)
        ? globalThis.Number(object.startHeight)
        : isSet(object.start_height)
          ? globalThis.Number(object.start_height)
          : 0,
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
      slashTimes: isSet(object.slashTimes)
        ? globalThis.Number(object.slashTimes)
        : isSet(object.slash_times)
          ? globalThis.Number(object.slash_times)
          : 0,
    }
  },

  toJSON(message: Relayer): unknown {
    const obj: any = {}
    if (message.relayerAddress !== '') {
      obj.relayerAddress = message.relayerAddress
    }
    if (message.externalAddress !== '') {
      obj.externalAddress = message.externalAddress
    }
    if (message.delegateAmount !== '') {
      obj.delegateAmount = message.delegateAmount
    }
    if (message.startHeight !== 0) {
      obj.startHeight = Math.round(message.startHeight)
    }
    if (message.online !== false) {
      obj.online = message.online
    }
    if (message.slashTimes !== 0) {
      obj.slashTimes = Math.round(message.slashTimes)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<Relayer>, I>>(base?: I): Relayer {
    return Relayer.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<Relayer>, I>>(object: I): Relayer {
    const message = createBaseRelayer()
    message.relayerAddress = object.relayerAddress ?? ''
    message.externalAddress = object.externalAddress ?? ''
    message.delegateAmount = object.delegateAmount ?? ''
    message.startHeight = object.startHeight ?? 0
    message.online = object.online ?? false
    message.slashTimes = object.slashTimes ?? 0
    return message
  },
}

function createBaseBridgeValidator(): BridgeValidator {
  return { power: 0, externalAddress: '' }
}

export const BridgeValidator: MessageFns<BridgeValidator> = {
  encode(message: BridgeValidator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.power !== 0) {
      writer.uint32(8).uint64(message.power)
    }
    if (message.externalAddress !== '') {
      writer.uint32(18).string(message.externalAddress)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeValidator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseBridgeValidator()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.power = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.externalAddress = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): BridgeValidator {
    return {
      power: isSet(object.power) ? globalThis.Number(object.power) : 0,
      externalAddress: isSet(object.externalAddress)
        ? globalThis.String(object.externalAddress)
        : isSet(object.external_address)
          ? globalThis.String(object.external_address)
          : '',
    }
  },

  toJSON(message: BridgeValidator): unknown {
    const obj: any = {}
    if (message.power !== 0) {
      obj.power = Math.round(message.power)
    }
    if (message.externalAddress !== '') {
      obj.externalAddress = message.externalAddress
    }
    return obj
  },

  create<I extends Exact<DeepPartial<BridgeValidator>, I>>(base?: I): BridgeValidator {
    return BridgeValidator.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<BridgeValidator>, I>>(object: I): BridgeValidator {
    const message = createBaseBridgeValidator()
    message.power = object.power ?? 0
    message.externalAddress = object.externalAddress ?? ''
    return message
  },
}

function createBaseRelayerSet(): RelayerSet {
  return { nonce: 0, members: [], height: 0 }
}

export const RelayerSet: MessageFns<RelayerSet> = {
  encode(message: RelayerSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nonce !== 0) {
      writer.uint32(8).uint64(message.nonce)
    }
    for (const v of message.members) {
      BridgeValidator.encode(v!, writer.uint32(18).fork()).join()
    }
    if (message.height !== 0) {
      writer.uint32(24).uint64(message.height)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayerSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRelayerSet()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.nonce = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.members.push(BridgeValidator.decode(reader, reader.uint32()))
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.height = longToNumber(reader.uint64())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): RelayerSet {
    return {
      nonce: isSet(object.nonce) ? globalThis.Number(object.nonce) : 0,
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => BridgeValidator.fromJSON(e))
        : [],
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    }
  },

  toJSON(message: RelayerSet): unknown {
    const obj: any = {}
    if (message.nonce !== 0) {
      obj.nonce = Math.round(message.nonce)
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => BridgeValidator.toJSON(e))
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<RelayerSet>, I>>(base?: I): RelayerSet {
    return RelayerSet.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<RelayerSet>, I>>(object: I): RelayerSet {
    const message = createBaseRelayerSet()
    message.nonce = object.nonce ?? 0
    message.members = object.members?.map((e) => BridgeValidator.fromPartial(e)) || []
    message.height = object.height ?? 0
    return message
  },
}

function createBaseLastObservedBlockHeight(): LastObservedBlockHeight {
  return { externalBlockHeight: 0, blockHeight: 0 }
}

export const LastObservedBlockHeight: MessageFns<LastObservedBlockHeight> = {
  encode(
    message: LastObservedBlockHeight,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.externalBlockHeight !== 0) {
      writer.uint32(8).uint64(message.externalBlockHeight)
    }
    if (message.blockHeight !== 0) {
      writer.uint32(16).uint64(message.blockHeight)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LastObservedBlockHeight {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseLastObservedBlockHeight()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.externalBlockHeight = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.blockHeight = longToNumber(reader.uint64())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): LastObservedBlockHeight {
    return {
      externalBlockHeight: isSet(object.externalBlockHeight)
        ? globalThis.Number(object.externalBlockHeight)
        : isSet(object.external_block_height)
          ? globalThis.Number(object.external_block_height)
          : 0,
      blockHeight: isSet(object.blockHeight)
        ? globalThis.Number(object.blockHeight)
        : isSet(object.block_height)
          ? globalThis.Number(object.block_height)
          : 0,
    }
  },

  toJSON(message: LastObservedBlockHeight): unknown {
    const obj: any = {}
    if (message.externalBlockHeight !== 0) {
      obj.externalBlockHeight = Math.round(message.externalBlockHeight)
    }
    if (message.blockHeight !== 0) {
      obj.blockHeight = Math.round(message.blockHeight)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<LastObservedBlockHeight>, I>>(
    base?: I,
  ): LastObservedBlockHeight {
    return LastObservedBlockHeight.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<LastObservedBlockHeight>, I>>(
    object: I,
  ): LastObservedBlockHeight {
    const message = createBaseLastObservedBlockHeight()
    message.externalBlockHeight = object.externalBlockHeight ?? 0
    message.blockHeight = object.blockHeight ?? 0
    return message
  },
}

function createBaseBridgeToken(): BridgeToken {
  return { contractAddress: '', denom: '', name: '', symbol: '', decimal: 0, supply: '' }
}

export const BridgeToken: MessageFns<BridgeToken> = {
  encode(message: BridgeToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contractAddress !== '') {
      writer.uint32(10).string(message.contractAddress)
    }
    if (message.denom !== '') {
      writer.uint32(18).string(message.denom)
    }
    if (message.name !== '') {
      writer.uint32(26).string(message.name)
    }
    if (message.symbol !== '') {
      writer.uint32(34).string(message.symbol)
    }
    if (message.decimal !== 0) {
      writer.uint32(40).uint64(message.decimal)
    }
    if (message.supply !== '') {
      writer.uint32(50).string(message.supply)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseBridgeToken()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.contractAddress = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.denom = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.name = reader.string()
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.symbol = reader.string()
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.decimal = longToNumber(reader.uint64())
          continue
        }
        case 6: {
          if (tag !== 50) {
            break
          }

          message.supply = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): BridgeToken {
    return {
      contractAddress: isSet(object.contractAddress)
        ? globalThis.String(object.contractAddress)
        : isSet(object.contract_address)
          ? globalThis.String(object.contract_address)
          : '',
      denom: isSet(object.denom) ? globalThis.String(object.denom) : '',
      name: isSet(object.name) ? globalThis.String(object.name) : '',
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : '',
      decimal: isSet(object.decimal) ? globalThis.Number(object.decimal) : 0,
      supply: isSet(object.supply) ? globalThis.String(object.supply) : '',
    }
  },

  toJSON(message: BridgeToken): unknown {
    const obj: any = {}
    if (message.contractAddress !== '') {
      obj.contractAddress = message.contractAddress
    }
    if (message.denom !== '') {
      obj.denom = message.denom
    }
    if (message.name !== '') {
      obj.name = message.name
    }
    if (message.symbol !== '') {
      obj.symbol = message.symbol
    }
    if (message.decimal !== 0) {
      obj.decimal = Math.round(message.decimal)
    }
    if (message.supply !== '') {
      obj.supply = message.supply
    }
    return obj
  },

  create<I extends Exact<DeepPartial<BridgeToken>, I>>(base?: I): BridgeToken {
    return BridgeToken.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<BridgeToken>, I>>(object: I): BridgeToken {
    const message = createBaseBridgeToken()
    message.contractAddress = object.contractAddress ?? ''
    message.denom = object.denom ?? ''
    message.name = object.name ?? ''
    message.symbol = object.symbol ?? ''
    message.decimal = object.decimal ?? 0
    message.supply = object.supply ?? ''
    return message
  },
}

function createBaseAttestation(): Attestation {
  return { observed: false, votes: [], height: 0, claim: undefined }
}

export const Attestation: MessageFns<Attestation> = {
  encode(message: Attestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.observed !== false) {
      writer.uint32(8).bool(message.observed)
    }
    for (const v of message.votes) {
      writer.uint32(18).string(v!)
    }
    if (message.height !== 0) {
      writer.uint32(24).uint64(message.height)
    }
    if (message.claim !== undefined) {
      Any.encode(message.claim, writer.uint32(34).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Attestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseAttestation()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.observed = reader.bool()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.votes.push(reader.string())
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.height = longToNumber(reader.uint64())
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.claim = Any.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): Attestation {
    return {
      observed: isSet(object.observed) ? globalThis.Boolean(object.observed) : false,
      votes: globalThis.Array.isArray(object?.votes)
        ? object.votes.map((e: any) => globalThis.String(e))
        : [],
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      claim: isSet(object.claim) ? Any.fromJSON(object.claim) : undefined,
    }
  },

  toJSON(message: Attestation): unknown {
    const obj: any = {}
    if (message.observed !== false) {
      obj.observed = message.observed
    }
    if (message.votes?.length) {
      obj.votes = message.votes
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height)
    }
    if (message.claim !== undefined) {
      obj.claim = Any.toJSON(message.claim)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<Attestation>, I>>(base?: I): Attestation {
    return Attestation.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<Attestation>, I>>(object: I): Attestation {
    const message = createBaseAttestation()
    message.observed = object.observed ?? false
    message.votes = object.votes?.map((e) => e) || []
    message.height = object.height ?? 0
    message.claim =
      object.claim !== undefined && object.claim !== null
        ? Any.fromPartial(object.claim)
        : undefined
    return message
  },
}

function createBaseOutgoingTxBatch(): OutgoingTxBatch {
  return {
    batchNonce: 0,
    batchTimeout: 0,
    transactions: [],
    tokenContract: '',
    block: 0,
    feeReceive: '',
  }
}

export const OutgoingTxBatch: MessageFns<OutgoingTxBatch> = {
  encode(message: OutgoingTxBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchNonce !== 0) {
      writer.uint32(8).uint64(message.batchNonce)
    }
    if (message.batchTimeout !== 0) {
      writer.uint32(16).uint64(message.batchTimeout)
    }
    for (const v of message.transactions) {
      OutgoingTransferTx.encode(v!, writer.uint32(26).fork()).join()
    }
    if (message.tokenContract !== '') {
      writer.uint32(34).string(message.tokenContract)
    }
    if (message.block !== 0) {
      writer.uint32(40).uint64(message.block)
    }
    if (message.feeReceive !== '') {
      writer.uint32(50).string(message.feeReceive)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OutgoingTxBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseOutgoingTxBatch()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.batchNonce = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.batchTimeout = longToNumber(reader.uint64())
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.transactions.push(OutgoingTransferTx.decode(reader, reader.uint32()))
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.tokenContract = reader.string()
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.block = longToNumber(reader.uint64())
          continue
        }
        case 6: {
          if (tag !== 50) {
            break
          }

          message.feeReceive = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): OutgoingTxBatch {
    return {
      batchNonce: isSet(object.batchNonce)
        ? globalThis.Number(object.batchNonce)
        : isSet(object.batch_nonce)
          ? globalThis.Number(object.batch_nonce)
          : 0,
      batchTimeout: isSet(object.batchTimeout)
        ? globalThis.Number(object.batchTimeout)
        : isSet(object.batch_timeout)
          ? globalThis.Number(object.batch_timeout)
          : 0,
      transactions: globalThis.Array.isArray(object?.transactions)
        ? object.transactions.map((e: any) => OutgoingTransferTx.fromJSON(e))
        : [],
      tokenContract: isSet(object.tokenContract)
        ? globalThis.String(object.tokenContract)
        : isSet(object.token_contract)
          ? globalThis.String(object.token_contract)
          : '',
      block: isSet(object.block) ? globalThis.Number(object.block) : 0,
      feeReceive: isSet(object.feeReceive) ? globalThis.String(object.feeReceive) : '',
    }
  },

  toJSON(message: OutgoingTxBatch): unknown {
    const obj: any = {}
    if (message.batchNonce !== 0) {
      obj.batchNonce = Math.round(message.batchNonce)
    }
    if (message.batchTimeout !== 0) {
      obj.batchTimeout = Math.round(message.batchTimeout)
    }
    if (message.transactions?.length) {
      obj.transactions = message.transactions.map((e) => OutgoingTransferTx.toJSON(e))
    }
    if (message.tokenContract !== '') {
      obj.tokenContract = message.tokenContract
    }
    if (message.block !== 0) {
      obj.block = Math.round(message.block)
    }
    if (message.feeReceive !== '') {
      obj.feeReceive = message.feeReceive
    }
    return obj
  },

  create<I extends Exact<DeepPartial<OutgoingTxBatch>, I>>(base?: I): OutgoingTxBatch {
    return OutgoingTxBatch.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<OutgoingTxBatch>, I>>(object: I): OutgoingTxBatch {
    const message = createBaseOutgoingTxBatch()
    message.batchNonce = object.batchNonce ?? 0
    message.batchTimeout = object.batchTimeout ?? 0
    message.transactions = object.transactions?.map((e) => OutgoingTransferTx.fromPartial(e)) || []
    message.tokenContract = object.tokenContract ?? ''
    message.block = object.block ?? 0
    message.feeReceive = object.feeReceive ?? ''
    return message
  },
}

function createBaseOutgoingTransferTx(): OutgoingTransferTx {
  return { id: 0, sender: '', destAddress: '', token: undefined, fee: undefined }
}

export const OutgoingTransferTx: MessageFns<OutgoingTransferTx> = {
  encode(message: OutgoingTransferTx, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id)
    }
    if (message.sender !== '') {
      writer.uint32(18).string(message.sender)
    }
    if (message.destAddress !== '') {
      writer.uint32(26).string(message.destAddress)
    }
    if (message.token !== undefined) {
      ERC20Token.encode(message.token, writer.uint32(34).fork()).join()
    }
    if (message.fee !== undefined) {
      ERC20Token.encode(message.fee, writer.uint32(42).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OutgoingTransferTx {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseOutgoingTransferTx()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.id = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.sender = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.destAddress = reader.string()
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.token = ERC20Token.decode(reader, reader.uint32())
          continue
        }
        case 5: {
          if (tag !== 42) {
            break
          }

          message.fee = ERC20Token.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): OutgoingTransferTx {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      sender: isSet(object.sender) ? globalThis.String(object.sender) : '',
      destAddress: isSet(object.destAddress)
        ? globalThis.String(object.destAddress)
        : isSet(object.dest_address)
          ? globalThis.String(object.dest_address)
          : '',
      token: isSet(object.token) ? ERC20Token.fromJSON(object.token) : undefined,
      fee: isSet(object.fee) ? ERC20Token.fromJSON(object.fee) : undefined,
    }
  },

  toJSON(message: OutgoingTransferTx): unknown {
    const obj: any = {}
    if (message.id !== 0) {
      obj.id = Math.round(message.id)
    }
    if (message.sender !== '') {
      obj.sender = message.sender
    }
    if (message.destAddress !== '') {
      obj.destAddress = message.destAddress
    }
    if (message.token !== undefined) {
      obj.token = ERC20Token.toJSON(message.token)
    }
    if (message.fee !== undefined) {
      obj.fee = ERC20Token.toJSON(message.fee)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<OutgoingTransferTx>, I>>(base?: I): OutgoingTransferTx {
    return OutgoingTransferTx.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<OutgoingTransferTx>, I>>(object: I): OutgoingTransferTx {
    const message = createBaseOutgoingTransferTx()
    message.id = object.id ?? 0
    message.sender = object.sender ?? ''
    message.destAddress = object.destAddress ?? ''
    message.token =
      object.token !== undefined && object.token !== null
        ? ERC20Token.fromPartial(object.token)
        : undefined
    message.fee =
      object.fee !== undefined && object.fee !== null
        ? ERC20Token.fromPartial(object.fee)
        : undefined
    return message
  },
}

function createBaseERC20Token(): ERC20Token {
  return { contract: '', amount: '' }
}

export const ERC20Token: MessageFns<ERC20Token> = {
  encode(message: ERC20Token, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contract !== '') {
      writer.uint32(10).string(message.contract)
    }
    if (message.amount !== '') {
      writer.uint32(18).string(message.amount)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ERC20Token {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseERC20Token()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.contract = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.amount = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): ERC20Token {
    return {
      contract: isSet(object.contract) ? globalThis.String(object.contract) : '',
      amount: isSet(object.amount) ? globalThis.String(object.amount) : '',
    }
  },

  toJSON(message: ERC20Token): unknown {
    const obj: any = {}
    if (message.contract !== '') {
      obj.contract = message.contract
    }
    if (message.amount !== '') {
      obj.amount = message.amount
    }
    return obj
  },

  create<I extends Exact<DeepPartial<ERC20Token>, I>>(base?: I): ERC20Token {
    return ERC20Token.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<ERC20Token>, I>>(object: I): ERC20Token {
    const message = createBaseERC20Token()
    message.contract = object.contract ?? ''
    message.amount = object.amount ?? ''
    return message
  },
}

function createBaseIDSet(): IDSet {
  return { ids: [] }
}

export const IDSet: MessageFns<IDSet> = {
  encode(message: IDSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork()
    for (const v of message.ids) {
      writer.uint64(v)
    }
    writer.join()
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseIDSet()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.ids.push(longToNumber(reader.uint64()))

            continue
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos
            while (reader.pos < end2) {
              message.ids.push(longToNumber(reader.uint64()))
            }

            continue
          }

          break
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): IDSet {
    return {
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.Number(e))
        : [],
    }
  },

  toJSON(message: IDSet): unknown {
    const obj: any = {}
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Math.round(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<IDSet>, I>>(base?: I): IDSet {
    return IDSet.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<IDSet>, I>>(object: I): IDSet {
    const message = createBaseIDSet()
    message.ids = object.ids?.map((e) => e) || []
    return message
  },
}

function createBaseBatchFees(): BatchFees {
  return { tokenContract: '', totalFees: '', totalTxs: 0, totalAmount: '' }
}

export const BatchFees: MessageFns<BatchFees> = {
  encode(message: BatchFees, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenContract !== '') {
      writer.uint32(10).string(message.tokenContract)
    }
    if (message.totalFees !== '') {
      writer.uint32(18).string(message.totalFees)
    }
    if (message.totalTxs !== 0) {
      writer.uint32(24).uint64(message.totalTxs)
    }
    if (message.totalAmount !== '') {
      writer.uint32(34).string(message.totalAmount)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchFees {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseBatchFees()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.tokenContract = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.totalFees = reader.string()
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.totalTxs = longToNumber(reader.uint64())
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.totalAmount = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): BatchFees {
    return {
      tokenContract: isSet(object.tokenContract)
        ? globalThis.String(object.tokenContract)
        : isSet(object.token_contract)
          ? globalThis.String(object.token_contract)
          : '',
      totalFees: isSet(object.totalFees)
        ? globalThis.String(object.totalFees)
        : isSet(object.total_fees)
          ? globalThis.String(object.total_fees)
          : '',
      totalTxs: isSet(object.totalTxs)
        ? globalThis.Number(object.totalTxs)
        : isSet(object.total_txs)
          ? globalThis.Number(object.total_txs)
          : 0,
      totalAmount: isSet(object.totalAmount)
        ? globalThis.String(object.totalAmount)
        : isSet(object.total_amount)
          ? globalThis.String(object.total_amount)
          : '',
    }
  },

  toJSON(message: BatchFees): unknown {
    const obj: any = {}
    if (message.tokenContract !== '') {
      obj.tokenContract = message.tokenContract
    }
    if (message.totalFees !== '') {
      obj.totalFees = message.totalFees
    }
    if (message.totalTxs !== 0) {
      obj.totalTxs = Math.round(message.totalTxs)
    }
    if (message.totalAmount !== '') {
      obj.totalAmount = message.totalAmount
    }
    return obj
  },

  create<I extends Exact<DeepPartial<BatchFees>, I>>(base?: I): BatchFees {
    return BatchFees.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<BatchFees>, I>>(object: I): BatchFees {
    const message = createBaseBatchFees()
    message.tokenContract = object.tokenContract ?? ''
    message.totalFees = object.totalFees ?? ''
    message.totalTxs = object.totalTxs ?? 0
    message.totalAmount = object.totalAmount ?? ''
    return message
  },
}

function createBaseMinBatchFee(): MinBatchFee {
  return { tokenContract: '', baseFee: '' }
}

export const MinBatchFee: MessageFns<MinBatchFee> = {
  encode(message: MinBatchFee, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tokenContract !== '') {
      writer.uint32(10).string(message.tokenContract)
    }
    if (message.baseFee !== '') {
      writer.uint32(18).string(message.baseFee)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MinBatchFee {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMinBatchFee()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.tokenContract = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.baseFee = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): MinBatchFee {
    return {
      tokenContract: isSet(object.tokenContract)
        ? globalThis.String(object.tokenContract)
        : isSet(object.token_contract)
          ? globalThis.String(object.token_contract)
          : '',
      baseFee: isSet(object.baseFee) ? globalThis.String(object.baseFee) : '',
    }
  },

  toJSON(message: MinBatchFee): unknown {
    const obj: any = {}
    if (message.tokenContract !== '') {
      obj.tokenContract = message.tokenContract
    }
    if (message.baseFee !== '') {
      obj.baseFee = message.baseFee
    }
    return obj
  },

  create<I extends Exact<DeepPartial<MinBatchFee>, I>>(base?: I): MinBatchFee {
    return MinBatchFee.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<MinBatchFee>, I>>(object: I): MinBatchFee {
    const message = createBaseMinBatchFee()
    message.tokenContract = object.tokenContract ?? ''
    message.baseFee = object.baseFee ?? ''
    return message
  },
}

function createBaseParams(): Params {
  return {
    gravityId: '',
    averageBlockTime: 0,
    externalBatchTimeout: 0,
    averageExternalBlockTime: 0,
    signedWindow: 0,
    slashFraction: '',
    relayerSetUpdatePowerChangePercent: '',
    maxRelayers: 0,
    minDelegate: '',
    maxDelegate: '',
    maxSlashTimes: 0,
    maxSendToExternalUsdAmount: '',
  }
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gravityId !== '') {
      writer.uint32(10).string(message.gravityId)
    }
    if (message.averageBlockTime !== 0) {
      writer.uint32(16).uint64(message.averageBlockTime)
    }
    if (message.externalBatchTimeout !== 0) {
      writer.uint32(24).uint64(message.externalBatchTimeout)
    }
    if (message.averageExternalBlockTime !== 0) {
      writer.uint32(32).uint64(message.averageExternalBlockTime)
    }
    if (message.signedWindow !== 0) {
      writer.uint32(40).uint64(message.signedWindow)
    }
    if (message.slashFraction !== '') {
      writer.uint32(50).string(message.slashFraction)
    }
    if (message.relayerSetUpdatePowerChangePercent !== '') {
      writer.uint32(58).string(message.relayerSetUpdatePowerChangePercent)
    }
    if (message.maxRelayers !== 0) {
      writer.uint32(64).uint64(message.maxRelayers)
    }
    if (message.minDelegate !== '') {
      writer.uint32(74).string(message.minDelegate)
    }
    if (message.maxDelegate !== '') {
      writer.uint32(82).string(message.maxDelegate)
    }
    if (message.maxSlashTimes !== 0) {
      writer.uint32(88).uint64(message.maxSlashTimes)
    }
    if (message.maxSendToExternalUsdAmount !== '') {
      writer.uint32(98).string(message.maxSendToExternalUsdAmount)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseParams()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.gravityId = reader.string()
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.averageBlockTime = longToNumber(reader.uint64())
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.externalBatchTimeout = longToNumber(reader.uint64())
          continue
        }
        case 4: {
          if (tag !== 32) {
            break
          }

          message.averageExternalBlockTime = longToNumber(reader.uint64())
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.signedWindow = longToNumber(reader.uint64())
          continue
        }
        case 6: {
          if (tag !== 50) {
            break
          }

          message.slashFraction = reader.string()
          continue
        }
        case 7: {
          if (tag !== 58) {
            break
          }

          message.relayerSetUpdatePowerChangePercent = reader.string()
          continue
        }
        case 8: {
          if (tag !== 64) {
            break
          }

          message.maxRelayers = longToNumber(reader.uint64())
          continue
        }
        case 9: {
          if (tag !== 74) {
            break
          }

          message.minDelegate = reader.string()
          continue
        }
        case 10: {
          if (tag !== 82) {
            break
          }

          message.maxDelegate = reader.string()
          continue
        }
        case 11: {
          if (tag !== 88) {
            break
          }

          message.maxSlashTimes = longToNumber(reader.uint64())
          continue
        }
        case 12: {
          if (tag !== 98) {
            break
          }

          message.maxSendToExternalUsdAmount = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): Params {
    return {
      gravityId: isSet(object.gravityId)
        ? globalThis.String(object.gravityId)
        : isSet(object.gravity_id)
          ? globalThis.String(object.gravity_id)
          : '',
      averageBlockTime: isSet(object.averageBlockTime)
        ? globalThis.Number(object.averageBlockTime)
        : isSet(object.average_block_time)
          ? globalThis.Number(object.average_block_time)
          : 0,
      externalBatchTimeout: isSet(object.externalBatchTimeout)
        ? globalThis.Number(object.externalBatchTimeout)
        : isSet(object.external_batch_timeout)
          ? globalThis.Number(object.external_batch_timeout)
          : 0,
      averageExternalBlockTime: isSet(object.averageExternalBlockTime)
        ? globalThis.Number(object.averageExternalBlockTime)
        : isSet(object.average_external_block_time)
          ? globalThis.Number(object.average_external_block_time)
          : 0,
      signedWindow: isSet(object.signedWindow)
        ? globalThis.Number(object.signedWindow)
        : isSet(object.signed_window)
          ? globalThis.Number(object.signed_window)
          : 0,
      slashFraction: isSet(object.slashFraction)
        ? globalThis.String(object.slashFraction)
        : isSet(object.slash_fraction)
          ? globalThis.String(object.slash_fraction)
          : '',
      relayerSetUpdatePowerChangePercent: isSet(object.relayerSetUpdatePowerChangePercent)
        ? globalThis.String(object.relayerSetUpdatePowerChangePercent)
        : isSet(object.relayer_set_update_power_change_percent)
          ? globalThis.String(object.relayer_set_update_power_change_percent)
          : '',
      maxRelayers: isSet(object.maxRelayers)
        ? globalThis.Number(object.maxRelayers)
        : isSet(object.max_relayers)
          ? globalThis.Number(object.max_relayers)
          : 0,
      minDelegate: isSet(object.minDelegate)
        ? globalThis.String(object.minDelegate)
        : isSet(object.min_delegate)
          ? globalThis.String(object.min_delegate)
          : '',
      maxDelegate: isSet(object.maxDelegate)
        ? globalThis.String(object.maxDelegate)
        : isSet(object.max_delegate)
          ? globalThis.String(object.max_delegate)
          : '',
      maxSlashTimes: isSet(object.maxSlashTimes)
        ? globalThis.Number(object.maxSlashTimes)
        : isSet(object.max_slash_times)
          ? globalThis.Number(object.max_slash_times)
          : 0,
      maxSendToExternalUsdAmount: isSet(object.maxSendToExternalUsdAmount)
        ? globalThis.String(object.maxSendToExternalUsdAmount)
        : isSet(object.max_send_to_external_usd_amount)
          ? globalThis.String(object.max_send_to_external_usd_amount)
          : '',
    }
  },

  toJSON(message: Params): unknown {
    const obj: any = {}
    if (message.gravityId !== '') {
      obj.gravityId = message.gravityId
    }
    if (message.averageBlockTime !== 0) {
      obj.averageBlockTime = Math.round(message.averageBlockTime)
    }
    if (message.externalBatchTimeout !== 0) {
      obj.externalBatchTimeout = Math.round(message.externalBatchTimeout)
    }
    if (message.averageExternalBlockTime !== 0) {
      obj.averageExternalBlockTime = Math.round(message.averageExternalBlockTime)
    }
    if (message.signedWindow !== 0) {
      obj.signedWindow = Math.round(message.signedWindow)
    }
    if (message.slashFraction !== '') {
      obj.slashFraction = message.slashFraction
    }
    if (message.relayerSetUpdatePowerChangePercent !== '') {
      obj.relayerSetUpdatePowerChangePercent = message.relayerSetUpdatePowerChangePercent
    }
    if (message.maxRelayers !== 0) {
      obj.maxRelayers = Math.round(message.maxRelayers)
    }
    if (message.minDelegate !== '') {
      obj.minDelegate = message.minDelegate
    }
    if (message.maxDelegate !== '') {
      obj.maxDelegate = message.maxDelegate
    }
    if (message.maxSlashTimes !== 0) {
      obj.maxSlashTimes = Math.round(message.maxSlashTimes)
    }
    if (message.maxSendToExternalUsdAmount !== '') {
      obj.maxSendToExternalUsdAmount = message.maxSendToExternalUsdAmount
    }
    return obj
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams()
    message.gravityId = object.gravityId ?? ''
    message.averageBlockTime = object.averageBlockTime ?? 0
    message.externalBatchTimeout = object.externalBatchTimeout ?? 0
    message.averageExternalBlockTime = object.averageExternalBlockTime ?? 0
    message.signedWindow = object.signedWindow ?? 0
    message.slashFraction = object.slashFraction ?? ''
    message.relayerSetUpdatePowerChangePercent = object.relayerSetUpdatePowerChangePercent ?? ''
    message.maxRelayers = object.maxRelayers ?? 0
    message.minDelegate = object.minDelegate ?? ''
    message.maxDelegate = object.maxDelegate ?? ''
    message.maxSlashTimes = object.maxSlashTimes ?? 0
    message.maxSendToExternalUsdAmount = object.maxSendToExternalUsdAmount ?? ''
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
