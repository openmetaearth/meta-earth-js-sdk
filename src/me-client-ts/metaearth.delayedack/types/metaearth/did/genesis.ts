// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/did/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Credential } from './credential'
import { DidInfo } from './did'
import { FilterLogger } from './filter'
import { Service } from './issuer'

export const protobufPackage = 'metaearth.did'

/** GenesisState defines the kyc module's genesis state. */
export interface GenesisState {
  infos: DidInfo[]
  svcs: Service[]
  vcs: Credential[]
  flogs: FilterLogger[]
}

function createBaseGenesisState(): GenesisState {
  return { infos: [], svcs: [], vcs: [], flogs: [] }
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.infos) {
      DidInfo.encode(v!, writer.uint32(10).fork()).join()
    }
    for (const v of message.svcs) {
      Service.encode(v!, writer.uint32(18).fork()).join()
    }
    for (const v of message.vcs) {
      Credential.encode(v!, writer.uint32(26).fork()).join()
    }
    for (const v of message.flogs) {
      FilterLogger.encode(v!, writer.uint32(34).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseGenesisState()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.infos.push(DidInfo.decode(reader, reader.uint32()))
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.svcs.push(Service.decode(reader, reader.uint32()))
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.vcs.push(Credential.decode(reader, reader.uint32()))
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.flogs.push(FilterLogger.decode(reader, reader.uint32()))
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): GenesisState {
    return {
      infos: globalThis.Array.isArray(object?.infos)
        ? object.infos.map((e: any) => DidInfo.fromJSON(e))
        : [],
      svcs: globalThis.Array.isArray(object?.svcs)
        ? object.svcs.map((e: any) => Service.fromJSON(e))
        : [],
      vcs: globalThis.Array.isArray(object?.vcs)
        ? object.vcs.map((e: any) => Credential.fromJSON(e))
        : [],
      flogs: globalThis.Array.isArray(object?.flogs)
        ? object.flogs.map((e: any) => FilterLogger.fromJSON(e))
        : [],
    }
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {}
    if (message.infos?.length) {
      obj.infos = message.infos.map((e) => DidInfo.toJSON(e))
    }
    if (message.svcs?.length) {
      obj.svcs = message.svcs.map((e) => Service.toJSON(e))
    }
    if (message.vcs?.length) {
      obj.vcs = message.vcs.map((e) => Credential.toJSON(e))
    }
    if (message.flogs?.length) {
      obj.flogs = message.flogs.map((e) => FilterLogger.toJSON(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState()
    message.infos = object.infos?.map((e) => DidInfo.fromPartial(e)) || []
    message.svcs = object.svcs?.map((e) => Service.fromPartial(e)) || []
    message.vcs = object.vcs?.map((e) => Credential.fromPartial(e)) || []
    message.flogs = object.flogs?.map((e) => FilterLogger.fromPartial(e)) || []
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
