// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/did/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { PageRequest, PageResponse } from '../../cosmos/base/query/v1beta1/pagination'
import { Credential } from './credential'
import { DidDocument, DidInfo } from './did'
import { Service } from './issuer'

export const protobufPackage = 'metaearth.did'

export interface QueryDid {
  address: string
}

export interface QueryDidResponse {
  info: DidInfo | undefined
}

export interface QueryDidInfo {
  did: string
}

export interface QueryDidInfoResponse {
  info: DidInfo | undefined
}

export interface QueryDidInfos {
  pagination: PageRequest | undefined
}

export interface QueryDidInfosResponse {
  infos: DidInfo[]
  pagination: PageResponse | undefined
}

export interface QueryDidDocument {
  did: string
}

export interface QueryDidDocumentResponse {
  doc: DidDocument | undefined
}

export interface QueryService {
  sid: string
}

export interface QueryServiceResponse {
  service: Service | undefined
}

export interface QueryServices {
  pagination: PageRequest | undefined
}

export interface QueryServicesResponse {
  services: Service[]
  pagination: PageResponse | undefined
}

export interface QueryCredential {
  did: string
  sid: string
}

export interface QueryCredentialResponse {
  credential: Credential | undefined
}

export interface QueryCredentials {
  sid: string
  filter: Uint8Array
  pagination: PageRequest | undefined
}

export interface QueryCredentialsResponse {
  credentials: Credential[]
  pagination: PageResponse | undefined
}

function createBaseQueryDid(): QueryDid {
  return { address: '' }
}

export const QueryDid: MessageFns<QueryDid> = {
  encode(message: QueryDid, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== '') {
      writer.uint32(10).string(message.address)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDid {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDid()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.address = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDid {
    return { address: isSet(object.address) ? globalThis.String(object.address) : '' }
  },

  toJSON(message: QueryDid): unknown {
    const obj: any = {}
    if (message.address !== '') {
      obj.address = message.address
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDid>, I>>(base?: I): QueryDid {
    return QueryDid.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDid>, I>>(object: I): QueryDid {
    const message = createBaseQueryDid()
    message.address = object.address ?? ''
    return message
  },
}

function createBaseQueryDidResponse(): QueryDidResponse {
  return { info: undefined }
}

export const QueryDidResponse: MessageFns<QueryDidResponse> = {
  encode(message: QueryDidResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.info !== undefined) {
      DidInfo.encode(message.info, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.info = DidInfo.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidResponse {
    return { info: isSet(object.info) ? DidInfo.fromJSON(object.info) : undefined }
  },

  toJSON(message: QueryDidResponse): unknown {
    const obj: any = {}
    if (message.info !== undefined) {
      obj.info = DidInfo.toJSON(message.info)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidResponse>, I>>(base?: I): QueryDidResponse {
    return QueryDidResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidResponse>, I>>(object: I): QueryDidResponse {
    const message = createBaseQueryDidResponse()
    message.info =
      object.info !== undefined && object.info !== null
        ? DidInfo.fromPartial(object.info)
        : undefined
    return message
  },
}

function createBaseQueryDidInfo(): QueryDidInfo {
  return { did: '' }
}

export const QueryDidInfo: MessageFns<QueryDidInfo> = {
  encode(message: QueryDidInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.did !== '') {
      writer.uint32(10).string(message.did)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidInfo()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.did = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidInfo {
    return { did: isSet(object.did) ? globalThis.String(object.did) : '' }
  },

  toJSON(message: QueryDidInfo): unknown {
    const obj: any = {}
    if (message.did !== '') {
      obj.did = message.did
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidInfo>, I>>(base?: I): QueryDidInfo {
    return QueryDidInfo.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidInfo>, I>>(object: I): QueryDidInfo {
    const message = createBaseQueryDidInfo()
    message.did = object.did ?? ''
    return message
  },
}

function createBaseQueryDidInfoResponse(): QueryDidInfoResponse {
  return { info: undefined }
}

export const QueryDidInfoResponse: MessageFns<QueryDidInfoResponse> = {
  encode(message: QueryDidInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.info !== undefined) {
      DidInfo.encode(message.info, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidInfoResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.info = DidInfo.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidInfoResponse {
    return { info: isSet(object.info) ? DidInfo.fromJSON(object.info) : undefined }
  },

  toJSON(message: QueryDidInfoResponse): unknown {
    const obj: any = {}
    if (message.info !== undefined) {
      obj.info = DidInfo.toJSON(message.info)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidInfoResponse>, I>>(base?: I): QueryDidInfoResponse {
    return QueryDidInfoResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidInfoResponse>, I>>(
    object: I,
  ): QueryDidInfoResponse {
    const message = createBaseQueryDidInfoResponse()
    message.info =
      object.info !== undefined && object.info !== null
        ? DidInfo.fromPartial(object.info)
        : undefined
    return message
  },
}

function createBaseQueryDidInfos(): QueryDidInfos {
  return { pagination: undefined }
}

export const QueryDidInfos: MessageFns<QueryDidInfos> = {
  encode(message: QueryDidInfos, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidInfos {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidInfos()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.pagination = PageRequest.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidInfos {
    return {
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryDidInfos): unknown {
    const obj: any = {}
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidInfos>, I>>(base?: I): QueryDidInfos {
    return QueryDidInfos.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidInfos>, I>>(object: I): QueryDidInfos {
    const message = createBaseQueryDidInfos()
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageRequest.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryDidInfosResponse(): QueryDidInfosResponse {
  return { infos: [], pagination: undefined }
}

export const QueryDidInfosResponse: MessageFns<QueryDidInfosResponse> = {
  encode(message: QueryDidInfosResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.infos) {
      DidInfo.encode(v!, writer.uint32(10).fork()).join()
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidInfosResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidInfosResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.infos.push(DidInfo.decode(reader, reader.uint32()))
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.pagination = PageResponse.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidInfosResponse {
    return {
      infos: globalThis.Array.isArray(object?.infos)
        ? object.infos.map((e: any) => DidInfo.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryDidInfosResponse): unknown {
    const obj: any = {}
    if (message.infos?.length) {
      obj.infos = message.infos.map((e) => DidInfo.toJSON(e))
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidInfosResponse>, I>>(base?: I): QueryDidInfosResponse {
    return QueryDidInfosResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidInfosResponse>, I>>(
    object: I,
  ): QueryDidInfosResponse {
    const message = createBaseQueryDidInfosResponse()
    message.infos = object.infos?.map((e) => DidInfo.fromPartial(e)) || []
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageResponse.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryDidDocument(): QueryDidDocument {
  return { did: '' }
}

export const QueryDidDocument: MessageFns<QueryDidDocument> = {
  encode(message: QueryDidDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.did !== '') {
      writer.uint32(10).string(message.did)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidDocument()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.did = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidDocument {
    return { did: isSet(object.did) ? globalThis.String(object.did) : '' }
  },

  toJSON(message: QueryDidDocument): unknown {
    const obj: any = {}
    if (message.did !== '') {
      obj.did = message.did
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidDocument>, I>>(base?: I): QueryDidDocument {
    return QueryDidDocument.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidDocument>, I>>(object: I): QueryDidDocument {
    const message = createBaseQueryDidDocument()
    message.did = object.did ?? ''
    return message
  },
}

function createBaseQueryDidDocumentResponse(): QueryDidDocumentResponse {
  return { doc: undefined }
}

export const QueryDidDocumentResponse: MessageFns<QueryDidDocumentResponse> = {
  encode(
    message: QueryDidDocumentResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.doc !== undefined) {
      DidDocument.encode(message.doc, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDidDocumentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryDidDocumentResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.doc = DidDocument.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryDidDocumentResponse {
    return { doc: isSet(object.doc) ? DidDocument.fromJSON(object.doc) : undefined }
  },

  toJSON(message: QueryDidDocumentResponse): unknown {
    const obj: any = {}
    if (message.doc !== undefined) {
      obj.doc = DidDocument.toJSON(message.doc)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryDidDocumentResponse>, I>>(
    base?: I,
  ): QueryDidDocumentResponse {
    return QueryDidDocumentResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryDidDocumentResponse>, I>>(
    object: I,
  ): QueryDidDocumentResponse {
    const message = createBaseQueryDidDocumentResponse()
    message.doc =
      object.doc !== undefined && object.doc !== null
        ? DidDocument.fromPartial(object.doc)
        : undefined
    return message
  },
}

function createBaseQueryService(): QueryService {
  return { sid: '' }
}

export const QueryService: MessageFns<QueryService> = {
  encode(message: QueryService, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sid !== '') {
      writer.uint32(10).string(message.sid)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryService {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryService()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.sid = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryService {
    return { sid: isSet(object.sid) ? globalThis.String(object.sid) : '' }
  },

  toJSON(message: QueryService): unknown {
    const obj: any = {}
    if (message.sid !== '') {
      obj.sid = message.sid
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryService>, I>>(base?: I): QueryService {
    return QueryService.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryService>, I>>(object: I): QueryService {
    const message = createBaseQueryService()
    message.sid = object.sid ?? ''
    return message
  },
}

function createBaseQueryServiceResponse(): QueryServiceResponse {
  return { service: undefined }
}

export const QueryServiceResponse: MessageFns<QueryServiceResponse> = {
  encode(message: QueryServiceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== undefined) {
      Service.encode(message.service, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryServiceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryServiceResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.service = Service.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryServiceResponse {
    return { service: isSet(object.service) ? Service.fromJSON(object.service) : undefined }
  },

  toJSON(message: QueryServiceResponse): unknown {
    const obj: any = {}
    if (message.service !== undefined) {
      obj.service = Service.toJSON(message.service)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryServiceResponse>, I>>(base?: I): QueryServiceResponse {
    return QueryServiceResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryServiceResponse>, I>>(
    object: I,
  ): QueryServiceResponse {
    const message = createBaseQueryServiceResponse()
    message.service =
      object.service !== undefined && object.service !== null
        ? Service.fromPartial(object.service)
        : undefined
    return message
  },
}

function createBaseQueryServices(): QueryServices {
  return { pagination: undefined }
}

export const QueryServices: MessageFns<QueryServices> = {
  encode(message: QueryServices, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryServices {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryServices()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.pagination = PageRequest.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryServices {
    return {
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryServices): unknown {
    const obj: any = {}
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryServices>, I>>(base?: I): QueryServices {
    return QueryServices.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryServices>, I>>(object: I): QueryServices {
    const message = createBaseQueryServices()
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageRequest.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryServicesResponse(): QueryServicesResponse {
  return { services: [], pagination: undefined }
}

export const QueryServicesResponse: MessageFns<QueryServicesResponse> = {
  encode(message: QueryServicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.services) {
      Service.encode(v!, writer.uint32(10).fork()).join()
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryServicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryServicesResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.services.push(Service.decode(reader, reader.uint32()))
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.pagination = PageResponse.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryServicesResponse {
    return {
      services: globalThis.Array.isArray(object?.services)
        ? object.services.map((e: any) => Service.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryServicesResponse): unknown {
    const obj: any = {}
    if (message.services?.length) {
      obj.services = message.services.map((e) => Service.toJSON(e))
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryServicesResponse>, I>>(base?: I): QueryServicesResponse {
    return QueryServicesResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryServicesResponse>, I>>(
    object: I,
  ): QueryServicesResponse {
    const message = createBaseQueryServicesResponse()
    message.services = object.services?.map((e) => Service.fromPartial(e)) || []
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageResponse.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryCredential(): QueryCredential {
  return { did: '', sid: '' }
}

export const QueryCredential: MessageFns<QueryCredential> = {
  encode(message: QueryCredential, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.did !== '') {
      writer.uint32(10).string(message.did)
    }
    if (message.sid !== '') {
      writer.uint32(18).string(message.sid)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCredential {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryCredential()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.did = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.sid = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryCredential {
    return {
      did: isSet(object.did) ? globalThis.String(object.did) : '',
      sid: isSet(object.sid) ? globalThis.String(object.sid) : '',
    }
  },

  toJSON(message: QueryCredential): unknown {
    const obj: any = {}
    if (message.did !== '') {
      obj.did = message.did
    }
    if (message.sid !== '') {
      obj.sid = message.sid
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryCredential>, I>>(base?: I): QueryCredential {
    return QueryCredential.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryCredential>, I>>(object: I): QueryCredential {
    const message = createBaseQueryCredential()
    message.did = object.did ?? ''
    message.sid = object.sid ?? ''
    return message
  },
}

function createBaseQueryCredentialResponse(): QueryCredentialResponse {
  return { credential: undefined }
}

export const QueryCredentialResponse: MessageFns<QueryCredentialResponse> = {
  encode(
    message: QueryCredentialResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.credential !== undefined) {
      Credential.encode(message.credential, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCredentialResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryCredentialResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.credential = Credential.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryCredentialResponse {
    return {
      credential: isSet(object.credential) ? Credential.fromJSON(object.credential) : undefined,
    }
  },

  toJSON(message: QueryCredentialResponse): unknown {
    const obj: any = {}
    if (message.credential !== undefined) {
      obj.credential = Credential.toJSON(message.credential)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryCredentialResponse>, I>>(
    base?: I,
  ): QueryCredentialResponse {
    return QueryCredentialResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryCredentialResponse>, I>>(
    object: I,
  ): QueryCredentialResponse {
    const message = createBaseQueryCredentialResponse()
    message.credential =
      object.credential !== undefined && object.credential !== null
        ? Credential.fromPartial(object.credential)
        : undefined
    return message
  },
}

function createBaseQueryCredentials(): QueryCredentials {
  return { sid: '', filter: new Uint8Array(0), pagination: undefined }
}

export const QueryCredentials: MessageFns<QueryCredentials> = {
  encode(message: QueryCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sid !== '') {
      writer.uint32(10).string(message.sid)
    }
    if (message.filter.length !== 0) {
      writer.uint32(18).bytes(message.filter)
    }
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(26).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryCredentials()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.sid = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.filter = reader.bytes()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.pagination = PageRequest.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryCredentials {
    return {
      sid: isSet(object.sid) ? globalThis.String(object.sid) : '',
      filter: isSet(object.filter) ? bytesFromBase64(object.filter) : new Uint8Array(0),
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryCredentials): unknown {
    const obj: any = {}
    if (message.sid !== '') {
      obj.sid = message.sid
    }
    if (message.filter.length !== 0) {
      obj.filter = base64FromBytes(message.filter)
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryCredentials>, I>>(base?: I): QueryCredentials {
    return QueryCredentials.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryCredentials>, I>>(object: I): QueryCredentials {
    const message = createBaseQueryCredentials()
    message.sid = object.sid ?? ''
    message.filter = object.filter ?? new Uint8Array(0)
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageRequest.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryCredentialsResponse(): QueryCredentialsResponse {
  return { credentials: [], pagination: undefined }
}

export const QueryCredentialsResponse: MessageFns<QueryCredentialsResponse> = {
  encode(
    message: QueryCredentialsResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.credentials) {
      Credential.encode(v!, writer.uint32(10).fork()).join()
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCredentialsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryCredentialsResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.credentials.push(Credential.decode(reader, reader.uint32()))
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.pagination = PageResponse.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryCredentialsResponse {
    return {
      credentials: globalThis.Array.isArray(object?.credentials)
        ? object.credentials.map((e: any) => Credential.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryCredentialsResponse): unknown {
    const obj: any = {}
    if (message.credentials?.length) {
      obj.credentials = message.credentials.map((e) => Credential.toJSON(e))
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryCredentialsResponse>, I>>(
    base?: I,
  ): QueryCredentialsResponse {
    return QueryCredentialsResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryCredentialsResponse>, I>>(
    object: I,
  ): QueryCredentialsResponse {
    const message = createBaseQueryCredentialsResponse()
    message.credentials = object.credentials?.map((e) => Credential.fromPartial(e)) || []
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageResponse.fromPartial(object.pagination)
        : undefined
    return message
  },
}

export interface Query {
  /** query user did doc */
  Did(request: QueryDid): Promise<QueryDidResponse>
  DidInfo(request: QueryDidInfo): Promise<QueryDidInfoResponse>
  DidInfos(request: QueryDidInfos): Promise<QueryDidInfosResponse>
  DidDocument(request: QueryDidDocument): Promise<QueryDidDocumentResponse>
  Service(request: QueryService): Promise<QueryServiceResponse>
  Services(request: QueryServices): Promise<QueryServicesResponse>
  Credential(request: QueryCredential): Promise<QueryCredentialResponse>
  Credentials(request: QueryCredentials): Promise<QueryCredentialsResponse>
}

export const QueryServiceName = 'metaearth.did.Query'
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc
  private readonly service: string
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName
    this.rpc = rpc
    this.Did = this.Did.bind(this)
    this.DidInfo = this.DidInfo.bind(this)
    this.DidInfos = this.DidInfos.bind(this)
    this.DidDocument = this.DidDocument.bind(this)
    this.Service = this.Service.bind(this)
    this.Services = this.Services.bind(this)
    this.Credential = this.Credential.bind(this)
    this.Credentials = this.Credentials.bind(this)
  }
  Did(request: QueryDid): Promise<QueryDidResponse> {
    const data = QueryDid.encode(request).finish()
    const promise = this.rpc.request(this.service, 'Did', data)
    return promise.then((data) => QueryDidResponse.decode(new BinaryReader(data)))
  }

  DidInfo(request: QueryDidInfo): Promise<QueryDidInfoResponse> {
    const data = QueryDidInfo.encode(request).finish()
    const promise = this.rpc.request(this.service, 'DidInfo', data)
    return promise.then((data) => QueryDidInfoResponse.decode(new BinaryReader(data)))
  }

  DidInfos(request: QueryDidInfos): Promise<QueryDidInfosResponse> {
    const data = QueryDidInfos.encode(request).finish()
    const promise = this.rpc.request(this.service, 'DidInfos', data)
    return promise.then((data) => QueryDidInfosResponse.decode(new BinaryReader(data)))
  }

  DidDocument(request: QueryDidDocument): Promise<QueryDidDocumentResponse> {
    const data = QueryDidDocument.encode(request).finish()
    const promise = this.rpc.request(this.service, 'DidDocument', data)
    return promise.then((data) => QueryDidDocumentResponse.decode(new BinaryReader(data)))
  }

  Service(request: QueryService): Promise<QueryServiceResponse> {
    const data = QueryService.encode(request).finish()
    const promise = this.rpc.request(this.service, 'Service', data)
    return promise.then((data) => QueryServiceResponse.decode(new BinaryReader(data)))
  }

  Services(request: QueryServices): Promise<QueryServicesResponse> {
    const data = QueryServices.encode(request).finish()
    const promise = this.rpc.request(this.service, 'Services', data)
    return promise.then((data) => QueryServicesResponse.decode(new BinaryReader(data)))
  }

  Credential(request: QueryCredential): Promise<QueryCredentialResponse> {
    const data = QueryCredential.encode(request).finish()
    const promise = this.rpc.request(this.service, 'Credential', data)
    return promise.then((data) => QueryCredentialResponse.decode(new BinaryReader(data)))
  }

  Credentials(request: QueryCredentials): Promise<QueryCredentialsResponse> {
    const data = QueryCredentials.encode(request).finish()
    const promise = this.rpc.request(this.service, 'Credentials', data)
    return promise.then((data) => QueryCredentialsResponse.decode(new BinaryReader(data)))
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, 'base64'))
  } else {
    const bin = globalThis.atob(b64)
    const arr = new Uint8Array(bin.length)
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i)
    }
    return arr
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString('base64')
  } else {
    const bin: string[] = []
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte))
    })
    return globalThis.btoa(bin.join(''))
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
