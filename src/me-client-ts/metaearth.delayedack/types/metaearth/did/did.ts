// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/did/did.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Credential } from './credential'

export const protobufPackage = 'metaearth.did'

/** kyc level enum */
export enum KycLevel {
  KYC_LEVEL_NONE = 0,
  KYC_LEVEL_ONE = 1,
  KYC_LEVEL_TWO = 2,
  KYC_LEVEL_THREE = 3,
  KYC_LEVEL_FOUR = 4,
  KYC_LEVEL_FIVE = 5,
  UNRECOGNIZED = -1,
}

export function kycLevelFromJSON(object: any): KycLevel {
  switch (object) {
    case 0:
    case 'KYC_LEVEL_NONE':
      return KycLevel.KYC_LEVEL_NONE
    case 1:
    case 'KYC_LEVEL_ONE':
      return KycLevel.KYC_LEVEL_ONE
    case 2:
    case 'KYC_LEVEL_TWO':
      return KycLevel.KYC_LEVEL_TWO
    case 3:
    case 'KYC_LEVEL_THREE':
      return KycLevel.KYC_LEVEL_THREE
    case 4:
    case 'KYC_LEVEL_FOUR':
      return KycLevel.KYC_LEVEL_FOUR
    case 5:
    case 'KYC_LEVEL_FIVE':
      return KycLevel.KYC_LEVEL_FIVE
    case -1:
    case 'UNRECOGNIZED':
    default:
      return KycLevel.UNRECOGNIZED
  }
}

export function kycLevelToJSON(object: KycLevel): string {
  switch (object) {
    case KycLevel.KYC_LEVEL_NONE:
      return 'KYC_LEVEL_NONE'
    case KycLevel.KYC_LEVEL_ONE:
      return 'KYC_LEVEL_ONE'
    case KycLevel.KYC_LEVEL_TWO:
      return 'KYC_LEVEL_TWO'
    case KycLevel.KYC_LEVEL_THREE:
      return 'KYC_LEVEL_THREE'
    case KycLevel.KYC_LEVEL_FOUR:
      return 'KYC_LEVEL_FOUR'
    case KycLevel.KYC_LEVEL_FIVE:
      return 'KYC_LEVEL_FIVE'
    case KycLevel.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED'
  }
}

/** did status enum */
export enum DidStatus {
  DID_STATUS_INACTIVE = 0,
  DID_STATUS_ACTIVE = 1,
  UNRECOGNIZED = -1,
}

export function didStatusFromJSON(object: any): DidStatus {
  switch (object) {
    case 0:
    case 'DID_STATUS_INACTIVE':
      return DidStatus.DID_STATUS_INACTIVE
    case 1:
    case 'DID_STATUS_ACTIVE':
      return DidStatus.DID_STATUS_ACTIVE
    case -1:
    case 'UNRECOGNIZED':
    default:
      return DidStatus.UNRECOGNIZED
  }
}

export function didStatusToJSON(object: DidStatus): string {
  switch (object) {
    case DidStatus.DID_STATUS_INACTIVE:
      return 'DID_STATUS_INACTIVE'
    case DidStatus.DID_STATUS_ACTIVE:
      return 'DID_STATUS_ACTIVE'
    case DidStatus.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED'
  }
}

export interface DidInfo {
  /** same as the MEID */
  did: string
  address: string
  /**
   * public_key is mapped to the user address
   * the issuer will use public_key to encrypt the user's certificate to ensure
   * the privacy of the off-chain certificate
   */
  pubkey: string
  status: DidStatus
  /** unused! */
  regionId: string
  kycLevel: KycLevel
  subAccount: string
}

/** did document */
export interface DidDocument {
  info: DidInfo | undefined
  vcs: Credential[]
}

function createBaseDidInfo(): DidInfo {
  return { did: '', address: '', pubkey: '', status: 0, regionId: '', kycLevel: 0, subAccount: '' }
}

export const DidInfo: MessageFns<DidInfo> = {
  encode(message: DidInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.did !== '') {
      writer.uint32(10).string(message.did)
    }
    if (message.address !== '') {
      writer.uint32(18).string(message.address)
    }
    if (message.pubkey !== '') {
      writer.uint32(26).string(message.pubkey)
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status)
    }
    if (message.regionId !== '') {
      writer.uint32(42).string(message.regionId)
    }
    if (message.kycLevel !== 0) {
      writer.uint32(48).int32(message.kycLevel)
    }
    if (message.subAccount !== '') {
      writer.uint32(58).string(message.subAccount)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DidInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseDidInfo()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.did = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.address = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.pubkey = reader.string()
          continue
        }
        case 4: {
          if (tag !== 32) {
            break
          }

          message.status = reader.int32() as any
          continue
        }
        case 5: {
          if (tag !== 42) {
            break
          }

          message.regionId = reader.string()
          continue
        }
        case 6: {
          if (tag !== 48) {
            break
          }

          message.kycLevel = reader.int32() as any
          continue
        }
        case 7: {
          if (tag !== 58) {
            break
          }

          message.subAccount = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): DidInfo {
    return {
      did: isSet(object.did) ? globalThis.String(object.did) : '',
      address: isSet(object.address) ? globalThis.String(object.address) : '',
      pubkey: isSet(object.pubkey) ? globalThis.String(object.pubkey) : '',
      status: isSet(object.status) ? didStatusFromJSON(object.status) : 0,
      regionId: isSet(object.regionId) ? globalThis.String(object.regionId) : '',
      kycLevel: isSet(object.kycLevel) ? kycLevelFromJSON(object.kycLevel) : 0,
      subAccount: isSet(object.subAccount)
        ? globalThis.String(object.subAccount)
        : isSet(object.sub_account)
          ? globalThis.String(object.sub_account)
          : '',
    }
  },

  toJSON(message: DidInfo): unknown {
    const obj: any = {}
    if (message.did !== '') {
      obj.did = message.did
    }
    if (message.address !== '') {
      obj.address = message.address
    }
    if (message.pubkey !== '') {
      obj.pubkey = message.pubkey
    }
    if (message.status !== 0) {
      obj.status = didStatusToJSON(message.status)
    }
    if (message.regionId !== '') {
      obj.regionId = message.regionId
    }
    if (message.kycLevel !== 0) {
      obj.kycLevel = kycLevelToJSON(message.kycLevel)
    }
    if (message.subAccount !== '') {
      obj.subAccount = message.subAccount
    }
    return obj
  },

  create<I extends Exact<DeepPartial<DidInfo>, I>>(base?: I): DidInfo {
    return DidInfo.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<DidInfo>, I>>(object: I): DidInfo {
    const message = createBaseDidInfo()
    message.did = object.did ?? ''
    message.address = object.address ?? ''
    message.pubkey = object.pubkey ?? ''
    message.status = object.status ?? 0
    message.regionId = object.regionId ?? ''
    message.kycLevel = object.kycLevel ?? 0
    message.subAccount = object.subAccount ?? ''
    return message
  },
}

function createBaseDidDocument(): DidDocument {
  return { info: undefined, vcs: [] }
}

export const DidDocument: MessageFns<DidDocument> = {
  encode(message: DidDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.info !== undefined) {
      DidInfo.encode(message.info, writer.uint32(10).fork()).join()
    }
    for (const v of message.vcs) {
      Credential.encode(v!, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DidDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseDidDocument()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.info = DidInfo.decode(reader, reader.uint32())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.vcs.push(Credential.decode(reader, reader.uint32()))
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): DidDocument {
    return {
      info: isSet(object.info) ? DidInfo.fromJSON(object.info) : undefined,
      vcs: globalThis.Array.isArray(object?.vcs)
        ? object.vcs.map((e: any) => Credential.fromJSON(e))
        : [],
    }
  },

  toJSON(message: DidDocument): unknown {
    const obj: any = {}
    if (message.info !== undefined) {
      obj.info = DidInfo.toJSON(message.info)
    }
    if (message.vcs?.length) {
      obj.vcs = message.vcs.map((e) => Credential.toJSON(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<DidDocument>, I>>(base?: I): DidDocument {
    return DidDocument.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<DidDocument>, I>>(object: I): DidDocument {
    const message = createBaseDidDocument()
    message.info =
      object.info !== undefined && object.info !== null
        ? DidInfo.fromPartial(object.info)
        : undefined
    message.vcs = object.vcs?.map((e) => Credential.fromPartial(e)) || []
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
