// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/dao/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { PageRequest, PageResponse } from '../../cosmos/base/query/v1beta1/pagination'
import { DaoAddresses } from './dao'

export const protobufPackage = 'metaearth.dao'

export interface QueryGlobalDaoRequest {}

export interface QueryGlobalDaoResponse {
  daoAddresses: DaoAddresses | undefined
}

export interface QueryGlobalDaoFeePoolReq {}

export interface QueryGlobalDaoFeePoolResp {
  globalDaoFeePool: string
}

export interface QueryFreeGasAccountsReq {
  pagination: PageRequest | undefined
}

export interface QueryFreeGasAccountsResp {
  addresses: string[]
  pagination: PageResponse | undefined
}

export interface QueryIsFreeGasAccountReq {
  address: string
}

export interface QueryIsFreeGasAccountResp {
  isFree: boolean
}

function createBaseQueryGlobalDaoRequest(): QueryGlobalDaoRequest {
  return {}
}

export const QueryGlobalDaoRequest: MessageFns<QueryGlobalDaoRequest> = {
  encode(_: QueryGlobalDaoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGlobalDaoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryGlobalDaoRequest()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(_: any): QueryGlobalDaoRequest {
    return {}
  },

  toJSON(_: QueryGlobalDaoRequest): unknown {
    const obj: any = {}
    return obj
  },

  create<I extends Exact<DeepPartial<QueryGlobalDaoRequest>, I>>(base?: I): QueryGlobalDaoRequest {
    return QueryGlobalDaoRequest.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryGlobalDaoRequest>, I>>(_: I): QueryGlobalDaoRequest {
    const message = createBaseQueryGlobalDaoRequest()
    return message
  },
}

function createBaseQueryGlobalDaoResponse(): QueryGlobalDaoResponse {
  return { daoAddresses: undefined }
}

export const QueryGlobalDaoResponse: MessageFns<QueryGlobalDaoResponse> = {
  encode(message: QueryGlobalDaoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.daoAddresses !== undefined) {
      DaoAddresses.encode(message.daoAddresses, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGlobalDaoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryGlobalDaoResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.daoAddresses = DaoAddresses.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryGlobalDaoResponse {
    return {
      daoAddresses: isSet(object.daoAddresses)
        ? DaoAddresses.fromJSON(object.daoAddresses)
        : isSet(object.dao_addresses)
          ? DaoAddresses.fromJSON(object.dao_addresses)
          : undefined,
    }
  },

  toJSON(message: QueryGlobalDaoResponse): unknown {
    const obj: any = {}
    if (message.daoAddresses !== undefined) {
      obj.daoAddresses = DaoAddresses.toJSON(message.daoAddresses)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryGlobalDaoResponse>, I>>(
    base?: I,
  ): QueryGlobalDaoResponse {
    return QueryGlobalDaoResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryGlobalDaoResponse>, I>>(
    object: I,
  ): QueryGlobalDaoResponse {
    const message = createBaseQueryGlobalDaoResponse()
    message.daoAddresses =
      object.daoAddresses !== undefined && object.daoAddresses !== null
        ? DaoAddresses.fromPartial(object.daoAddresses)
        : undefined
    return message
  },
}

function createBaseQueryGlobalDaoFeePoolReq(): QueryGlobalDaoFeePoolReq {
  return {}
}

export const QueryGlobalDaoFeePoolReq: MessageFns<QueryGlobalDaoFeePoolReq> = {
  encode(_: QueryGlobalDaoFeePoolReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGlobalDaoFeePoolReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryGlobalDaoFeePoolReq()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(_: any): QueryGlobalDaoFeePoolReq {
    return {}
  },

  toJSON(_: QueryGlobalDaoFeePoolReq): unknown {
    const obj: any = {}
    return obj
  },

  create<I extends Exact<DeepPartial<QueryGlobalDaoFeePoolReq>, I>>(
    base?: I,
  ): QueryGlobalDaoFeePoolReq {
    return QueryGlobalDaoFeePoolReq.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryGlobalDaoFeePoolReq>, I>>(
    _: I,
  ): QueryGlobalDaoFeePoolReq {
    const message = createBaseQueryGlobalDaoFeePoolReq()
    return message
  },
}

function createBaseQueryGlobalDaoFeePoolResp(): QueryGlobalDaoFeePoolResp {
  return { globalDaoFeePool: '' }
}

export const QueryGlobalDaoFeePoolResp: MessageFns<QueryGlobalDaoFeePoolResp> = {
  encode(
    message: QueryGlobalDaoFeePoolResp,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.globalDaoFeePool !== '') {
      writer.uint32(10).string(message.globalDaoFeePool)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGlobalDaoFeePoolResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryGlobalDaoFeePoolResp()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.globalDaoFeePool = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryGlobalDaoFeePoolResp {
    return {
      globalDaoFeePool: isSet(object.globalDaoFeePool)
        ? globalThis.String(object.globalDaoFeePool)
        : isSet(object.global_dao_fee_pool)
          ? globalThis.String(object.global_dao_fee_pool)
          : '',
    }
  },

  toJSON(message: QueryGlobalDaoFeePoolResp): unknown {
    const obj: any = {}
    if (message.globalDaoFeePool !== '') {
      obj.globalDaoFeePool = message.globalDaoFeePool
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryGlobalDaoFeePoolResp>, I>>(
    base?: I,
  ): QueryGlobalDaoFeePoolResp {
    return QueryGlobalDaoFeePoolResp.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryGlobalDaoFeePoolResp>, I>>(
    object: I,
  ): QueryGlobalDaoFeePoolResp {
    const message = createBaseQueryGlobalDaoFeePoolResp()
    message.globalDaoFeePool = object.globalDaoFeePool ?? ''
    return message
  },
}

function createBaseQueryFreeGasAccountsReq(): QueryFreeGasAccountsReq {
  return { pagination: undefined }
}

export const QueryFreeGasAccountsReq: MessageFns<QueryFreeGasAccountsReq> = {
  encode(
    message: QueryFreeGasAccountsReq,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryFreeGasAccountsReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryFreeGasAccountsReq()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break
          }

          message.pagination = PageRequest.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryFreeGasAccountsReq {
    return {
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryFreeGasAccountsReq): unknown {
    const obj: any = {}
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryFreeGasAccountsReq>, I>>(
    base?: I,
  ): QueryFreeGasAccountsReq {
    return QueryFreeGasAccountsReq.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryFreeGasAccountsReq>, I>>(
    object: I,
  ): QueryFreeGasAccountsReq {
    const message = createBaseQueryFreeGasAccountsReq()
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageRequest.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryFreeGasAccountsResp(): QueryFreeGasAccountsResp {
  return { addresses: [], pagination: undefined }
}

export const QueryFreeGasAccountsResp: MessageFns<QueryFreeGasAccountsResp> = {
  encode(
    message: QueryFreeGasAccountsResp,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.addresses) {
      writer.uint32(10).string(v!)
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryFreeGasAccountsResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryFreeGasAccountsResp()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.addresses.push(reader.string())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.pagination = PageResponse.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryFreeGasAccountsResp {
    return {
      addresses: globalThis.Array.isArray(object?.addresses)
        ? object.addresses.map((e: any) => globalThis.String(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    }
  },

  toJSON(message: QueryFreeGasAccountsResp): unknown {
    const obj: any = {}
    if (message.addresses?.length) {
      obj.addresses = message.addresses
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryFreeGasAccountsResp>, I>>(
    base?: I,
  ): QueryFreeGasAccountsResp {
    return QueryFreeGasAccountsResp.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryFreeGasAccountsResp>, I>>(
    object: I,
  ): QueryFreeGasAccountsResp {
    const message = createBaseQueryFreeGasAccountsResp()
    message.addresses = object.addresses?.map((e) => e) || []
    message.pagination =
      object.pagination !== undefined && object.pagination !== null
        ? PageResponse.fromPartial(object.pagination)
        : undefined
    return message
  },
}

function createBaseQueryIsFreeGasAccountReq(): QueryIsFreeGasAccountReq {
  return { address: '' }
}

export const QueryIsFreeGasAccountReq: MessageFns<QueryIsFreeGasAccountReq> = {
  encode(
    message: QueryIsFreeGasAccountReq,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.address !== '') {
      writer.uint32(10).string(message.address)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryIsFreeGasAccountReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryIsFreeGasAccountReq()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.address = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryIsFreeGasAccountReq {
    return { address: isSet(object.address) ? globalThis.String(object.address) : '' }
  },

  toJSON(message: QueryIsFreeGasAccountReq): unknown {
    const obj: any = {}
    if (message.address !== '') {
      obj.address = message.address
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryIsFreeGasAccountReq>, I>>(
    base?: I,
  ): QueryIsFreeGasAccountReq {
    return QueryIsFreeGasAccountReq.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryIsFreeGasAccountReq>, I>>(
    object: I,
  ): QueryIsFreeGasAccountReq {
    const message = createBaseQueryIsFreeGasAccountReq()
    message.address = object.address ?? ''
    return message
  },
}

function createBaseQueryIsFreeGasAccountResp(): QueryIsFreeGasAccountResp {
  return { isFree: false }
}

export const QueryIsFreeGasAccountResp: MessageFns<QueryIsFreeGasAccountResp> = {
  encode(
    message: QueryIsFreeGasAccountResp,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.isFree !== false) {
      writer.uint32(8).bool(message.isFree)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryIsFreeGasAccountResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseQueryIsFreeGasAccountResp()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.isFree = reader.bool()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): QueryIsFreeGasAccountResp {
    return {
      isFree: isSet(object.isFree)
        ? globalThis.Boolean(object.isFree)
        : isSet(object.is_free)
          ? globalThis.Boolean(object.is_free)
          : false,
    }
  },

  toJSON(message: QueryIsFreeGasAccountResp): unknown {
    const obj: any = {}
    if (message.isFree !== false) {
      obj.isFree = message.isFree
    }
    return obj
  },

  create<I extends Exact<DeepPartial<QueryIsFreeGasAccountResp>, I>>(
    base?: I,
  ): QueryIsFreeGasAccountResp {
    return QueryIsFreeGasAccountResp.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<QueryIsFreeGasAccountResp>, I>>(
    object: I,
  ): QueryIsFreeGasAccountResp {
    const message = createBaseQueryIsFreeGasAccountResp()
    message.isFree = object.isFree ?? false
    return message
  },
}

/** Query defines the gRPC querier service. */
export interface Query {
  /** Queries a list of admin items. */
  GlobalDao(request: QueryGlobalDaoRequest): Promise<QueryGlobalDaoResponse>
  GlobalDaoFeePool(request: QueryGlobalDaoFeePoolReq): Promise<QueryGlobalDaoFeePoolResp>
  FreeGasAccounts(request: QueryFreeGasAccountsReq): Promise<QueryFreeGasAccountsResp>
  IsFreeGasAccount(request: QueryIsFreeGasAccountReq): Promise<QueryIsFreeGasAccountResp>
}

export const QueryServiceName = 'metaearth.dao.Query'
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc
  private readonly service: string
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName
    this.rpc = rpc
    this.GlobalDao = this.GlobalDao.bind(this)
    this.GlobalDaoFeePool = this.GlobalDaoFeePool.bind(this)
    this.FreeGasAccounts = this.FreeGasAccounts.bind(this)
    this.IsFreeGasAccount = this.IsFreeGasAccount.bind(this)
  }
  GlobalDao(request: QueryGlobalDaoRequest): Promise<QueryGlobalDaoResponse> {
    const data = QueryGlobalDaoRequest.encode(request).finish()
    const promise = this.rpc.request(this.service, 'GlobalDao', data)
    return promise.then((data) => QueryGlobalDaoResponse.decode(new BinaryReader(data)))
  }

  GlobalDaoFeePool(request: QueryGlobalDaoFeePoolReq): Promise<QueryGlobalDaoFeePoolResp> {
    const data = QueryGlobalDaoFeePoolReq.encode(request).finish()
    const promise = this.rpc.request(this.service, 'GlobalDaoFeePool', data)
    return promise.then((data) => QueryGlobalDaoFeePoolResp.decode(new BinaryReader(data)))
  }

  FreeGasAccounts(request: QueryFreeGasAccountsReq): Promise<QueryFreeGasAccountsResp> {
    const data = QueryFreeGasAccountsReq.encode(request).finish()
    const promise = this.rpc.request(this.service, 'FreeGasAccounts', data)
    return promise.then((data) => QueryFreeGasAccountsResp.decode(new BinaryReader(data)))
  }

  IsFreeGasAccount(request: QueryIsFreeGasAccountReq): Promise<QueryIsFreeGasAccountResp> {
    const data = QueryIsFreeGasAccountReq.encode(request).finish()
    const promise = this.rpc.request(this.service, 'IsFreeGasAccount', data)
    return promise.then((data) => QueryIsFreeGasAccountResp.decode(new BinaryReader(data)))
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
