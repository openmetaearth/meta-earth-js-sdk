// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/common/rollapp_packet.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Packet } from '../../ibc/core/channel/v1/channel'
import { Status, statusFromJSON, statusToJSON } from './status'

export const protobufPackage = 'metaearth.common'

export interface RollappPacket {
  rollappId: string
  packet: Packet | undefined
  acknowledgement: Uint8Array
  status: Status
  ProofHeight: number
  relayer: Uint8Array
  type: RollappPacket_Type
  /** stores the result of onAck, onTimeout or onRecv/writeAck */
  error: string
  /**
   * who was the original person who gets the money (recipient of ics20
   * transfer) of the packet?
   */
  originalTransferTarget: string
}

export enum RollappPacket_Type {
  ON_RECV = 0,
  ON_ACK = 1,
  ON_TIMEOUT = 2,
  UNDEFINED = -1,
}

export function rollappPacket_TypeFromJSON(object: any): RollappPacket_Type {
  switch (object) {
    case 0:
    case 'ON_RECV':
      return RollappPacket_Type.ON_RECV
    case 1:
    case 'ON_ACK':
      return RollappPacket_Type.ON_ACK
    case 2:
    case 'ON_TIMEOUT':
      return RollappPacket_Type.ON_TIMEOUT
    case -1:
    case 'UNDEFINED':
      return RollappPacket_Type.UNDEFINED
    default:
      return RollappPacket_Type.UNDEFINED
  }
}

export function rollappPacket_TypeToJSON(object: RollappPacket_Type): string {
  switch (object) {
    case RollappPacket_Type.ON_RECV:
      return 'ON_RECV'
    case RollappPacket_Type.ON_ACK:
      return 'ON_ACK'
    case RollappPacket_Type.ON_TIMEOUT:
      return 'ON_TIMEOUT'
    case RollappPacket_Type.UNDEFINED:
      return 'UNDEFINED'
    default:
      return 'UNDEFINED'
  }
}

function createBaseRollappPacket(): RollappPacket {
  return {
    rollappId: '',
    packet: undefined,
    acknowledgement: new Uint8Array(0),
    status: 0,
    ProofHeight: 0,
    relayer: new Uint8Array(0),
    type: 0,
    error: '',
    originalTransferTarget: '',
  }
}

export const RollappPacket: MessageFns<RollappPacket> = {
  encode(message: RollappPacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappId !== '') {
      writer.uint32(10).string(message.rollappId)
    }
    if (message.packet !== undefined) {
      Packet.encode(message.packet, writer.uint32(18).fork()).join()
    }
    if (message.acknowledgement.length !== 0) {
      writer.uint32(26).bytes(message.acknowledgement)
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status)
    }
    if (message.ProofHeight !== 0) {
      writer.uint32(40).uint64(message.ProofHeight)
    }
    if (message.relayer.length !== 0) {
      writer.uint32(50).bytes(message.relayer)
    }
    if (message.type !== 0) {
      writer.uint32(56).int32(message.type)
    }
    if (message.error !== '') {
      writer.uint32(66).string(message.error)
    }
    if (message.originalTransferTarget !== '') {
      writer.uint32(74).string(message.originalTransferTarget)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RollappPacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRollappPacket()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.rollappId = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.packet = Packet.decode(reader, reader.uint32())
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.acknowledgement = reader.bytes()
          continue
        }
        case 4: {
          if (tag !== 32) {
            break
          }

          message.status = reader.int32() as any
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.ProofHeight = longToNumber(reader.uint64())
          continue
        }
        case 6: {
          if (tag !== 50) {
            break
          }

          message.relayer = reader.bytes()
          continue
        }
        case 7: {
          if (tag !== 56) {
            break
          }

          message.type = reader.int32() as any
          continue
        }
        case 8: {
          if (tag !== 66) {
            break
          }

          message.error = reader.string()
          continue
        }
        case 9: {
          if (tag !== 74) {
            break
          }

          message.originalTransferTarget = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): RollappPacket {
    return {
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
          ? globalThis.String(object.rollapp_id)
          : '',
      packet: isSet(object.packet) ? Packet.fromJSON(object.packet) : undefined,
      acknowledgement: isSet(object.acknowledgement)
        ? bytesFromBase64(object.acknowledgement)
        : new Uint8Array(0),
      status: isSet(object.status) ? statusFromJSON(object.status) : 0,
      ProofHeight: isSet(object.ProofHeight) ? globalThis.Number(object.ProofHeight) : 0,
      relayer: isSet(object.relayer) ? bytesFromBase64(object.relayer) : new Uint8Array(0),
      type: isSet(object.type) ? rollappPacket_TypeFromJSON(object.type) : 0,
      error: isSet(object.error) ? globalThis.String(object.error) : '',
      originalTransferTarget: isSet(object.originalTransferTarget)
        ? globalThis.String(object.originalTransferTarget)
        : isSet(object.original_transfer_target)
          ? globalThis.String(object.original_transfer_target)
          : '',
    }
  },

  toJSON(message: RollappPacket): unknown {
    const obj: any = {}
    if (message.rollappId !== '') {
      obj.rollappId = message.rollappId
    }
    if (message.packet !== undefined) {
      obj.packet = Packet.toJSON(message.packet)
    }
    if (message.acknowledgement.length !== 0) {
      obj.acknowledgement = base64FromBytes(message.acknowledgement)
    }
    if (message.status !== 0) {
      obj.status = statusToJSON(message.status)
    }
    if (message.ProofHeight !== 0) {
      obj.ProofHeight = Math.round(message.ProofHeight)
    }
    if (message.relayer.length !== 0) {
      obj.relayer = base64FromBytes(message.relayer)
    }
    if (message.type !== 0) {
      obj.type = rollappPacket_TypeToJSON(message.type)
    }
    if (message.error !== '') {
      obj.error = message.error
    }
    if (message.originalTransferTarget !== '') {
      obj.originalTransferTarget = message.originalTransferTarget
    }
    return obj
  },

  create<I extends Exact<DeepPartial<RollappPacket>, I>>(base?: I): RollappPacket {
    return RollappPacket.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<RollappPacket>, I>>(object: I): RollappPacket {
    const message = createBaseRollappPacket()
    message.rollappId = object.rollappId ?? ''
    message.packet =
      object.packet !== undefined && object.packet !== null
        ? Packet.fromPartial(object.packet)
        : undefined
    message.acknowledgement = object.acknowledgement ?? new Uint8Array(0)
    message.status = object.status ?? 0
    message.ProofHeight = object.ProofHeight ?? 0
    message.relayer = object.relayer ?? new Uint8Array(0)
    message.type = object.type ?? 0
    message.error = object.error ?? ''
    message.originalTransferTarget = object.originalTransferTarget ?? ''
    return message
  },
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, 'base64'))
  } else {
    const bin = globalThis.atob(b64)
    const arr = new Uint8Array(bin.length)
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i)
    }
    return arr
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString('base64')
  } else {
    const bin: string[] = []
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte))
    })
    return globalThis.btoa(bin.join(''))
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
