// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/kyc/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { DidInfo } from '../did/did'
import { KycEventSeq } from './kyc_event_seq'

export const protobufPackage = 'metaearth.kyc'

/** GenesisState defines the kyc module's genesis state. */
export interface GenesisState {
  issuers: DidInfo[]
  kycEventSeq: KycEventSeq | undefined
}

function createBaseGenesisState(): GenesisState {
  return { issuers: [], kycEventSeq: undefined }
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.issuers) {
      DidInfo.encode(v!, writer.uint32(10).fork()).join()
    }
    if (message.kycEventSeq !== undefined) {
      KycEventSeq.encode(message.kycEventSeq, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseGenesisState()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.issuers.push(DidInfo.decode(reader, reader.uint32()))
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.kycEventSeq = KycEventSeq.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): GenesisState {
    return {
      issuers: globalThis.Array.isArray(object?.issuers)
        ? object.issuers.map((e: any) => DidInfo.fromJSON(e))
        : [],
      kycEventSeq: isSet(object.kycEventSeq) ? KycEventSeq.fromJSON(object.kycEventSeq) : undefined,
    }
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {}
    if (message.issuers?.length) {
      obj.issuers = message.issuers.map((e) => DidInfo.toJSON(e))
    }
    if (message.kycEventSeq !== undefined) {
      obj.kycEventSeq = KycEventSeq.toJSON(message.kycEventSeq)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState()
    message.issuers = object.issuers?.map((e) => DidInfo.fromPartial(e)) || []
    message.kycEventSeq =
      object.kycEventSeq !== undefined && object.kycEventSeq !== null
        ? KycEventSeq.fromPartial(object.kycEventSeq)
        : undefined
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
