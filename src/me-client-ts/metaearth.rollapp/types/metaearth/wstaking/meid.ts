// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/wstaking/meid.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.wstaking'

export interface Meid {
  account: string
  creator: string
  regionId: string
  regionName: string
  RewardType: number
}

function createBaseMeid(): Meid {
  return { account: '', creator: '', regionId: '', regionName: '', RewardType: 0 }
}

export const Meid: MessageFns<Meid> = {
  encode(message: Meid, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== '') {
      writer.uint32(10).string(message.account)
    }
    if (message.creator !== '') {
      writer.uint32(18).string(message.creator)
    }
    if (message.regionId !== '') {
      writer.uint32(26).string(message.regionId)
    }
    if (message.regionName !== '') {
      writer.uint32(34).string(message.regionName)
    }
    if (message.RewardType !== 0) {
      writer.uint32(40).int32(message.RewardType)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Meid {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMeid()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.account = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.creator = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.regionId = reader.string()
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.regionName = reader.string()
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.RewardType = reader.int32()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): Meid {
    return {
      account: isSet(object.account) ? globalThis.String(object.account) : '',
      creator: isSet(object.creator) ? globalThis.String(object.creator) : '',
      regionId: isSet(object.regionId) ? globalThis.String(object.regionId) : '',
      regionName: isSet(object.regionName) ? globalThis.String(object.regionName) : '',
      RewardType: isSet(object.RewardType) ? globalThis.Number(object.RewardType) : 0,
    }
  },

  toJSON(message: Meid): unknown {
    const obj: any = {}
    if (message.account !== '') {
      obj.account = message.account
    }
    if (message.creator !== '') {
      obj.creator = message.creator
    }
    if (message.regionId !== '') {
      obj.regionId = message.regionId
    }
    if (message.regionName !== '') {
      obj.regionName = message.regionName
    }
    if (message.RewardType !== 0) {
      obj.RewardType = Math.round(message.RewardType)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<Meid>, I>>(base?: I): Meid {
    return Meid.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<Meid>, I>>(object: I): Meid {
    const message = createBaseMeid()
    message.account = object.account ?? ''
    message.creator = object.creator ?? ''
    message.regionId = object.regionId ?? ''
    message.regionName = object.regionName ?? ''
    message.RewardType = object.RewardType ?? 0
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
