// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/wstaking/record.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.wstaking'

export interface Record {
  recordNumber: string
  url: string
  from: string
}

export interface ReviewRecord {
  recordHash: string
  actionNumber: string
  recordResult: string
  reviewedAddress: string
}

function createBaseRecord(): Record {
  return { recordNumber: '', url: '', from: '' }
}

export const Record: MessageFns<Record> = {
  encode(message: Record, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recordNumber !== '') {
      writer.uint32(10).string(message.recordNumber)
    }
    if (message.url !== '') {
      writer.uint32(18).string(message.url)
    }
    if (message.from !== '') {
      writer.uint32(26).string(message.from)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Record {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRecord()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.recordNumber = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.url = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.from = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): Record {
    return {
      recordNumber: isSet(object.recordNumber) ? globalThis.String(object.recordNumber) : '',
      url: isSet(object.url) ? globalThis.String(object.url) : '',
      from: isSet(object.from) ? globalThis.String(object.from) : '',
    }
  },

  toJSON(message: Record): unknown {
    const obj: any = {}
    if (message.recordNumber !== '') {
      obj.recordNumber = message.recordNumber
    }
    if (message.url !== '') {
      obj.url = message.url
    }
    if (message.from !== '') {
      obj.from = message.from
    }
    return obj
  },

  create<I extends Exact<DeepPartial<Record>, I>>(base?: I): Record {
    return Record.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<Record>, I>>(object: I): Record {
    const message = createBaseRecord()
    message.recordNumber = object.recordNumber ?? ''
    message.url = object.url ?? ''
    message.from = object.from ?? ''
    return message
  },
}

function createBaseReviewRecord(): ReviewRecord {
  return { recordHash: '', actionNumber: '', recordResult: '', reviewedAddress: '' }
}

export const ReviewRecord: MessageFns<ReviewRecord> = {
  encode(message: ReviewRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recordHash !== '') {
      writer.uint32(10).string(message.recordHash)
    }
    if (message.actionNumber !== '') {
      writer.uint32(18).string(message.actionNumber)
    }
    if (message.recordResult !== '') {
      writer.uint32(26).string(message.recordResult)
    }
    if (message.reviewedAddress !== '') {
      writer.uint32(34).string(message.reviewedAddress)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReviewRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseReviewRecord()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.recordHash = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.actionNumber = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.recordResult = reader.string()
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.reviewedAddress = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): ReviewRecord {
    return {
      recordHash: isSet(object.recordHash) ? globalThis.String(object.recordHash) : '',
      actionNumber: isSet(object.actionNumber) ? globalThis.String(object.actionNumber) : '',
      recordResult: isSet(object.recordResult) ? globalThis.String(object.recordResult) : '',
      reviewedAddress: isSet(object.reviewedAddress)
        ? globalThis.String(object.reviewedAddress)
        : '',
    }
  },

  toJSON(message: ReviewRecord): unknown {
    const obj: any = {}
    if (message.recordHash !== '') {
      obj.recordHash = message.recordHash
    }
    if (message.actionNumber !== '') {
      obj.actionNumber = message.actionNumber
    }
    if (message.recordResult !== '') {
      obj.recordResult = message.recordResult
    }
    if (message.reviewedAddress !== '') {
      obj.reviewedAddress = message.reviewedAddress
    }
    return obj
  },

  create<I extends Exact<DeepPartial<ReviewRecord>, I>>(base?: I): ReviewRecord {
    return ReviewRecord.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<ReviewRecord>, I>>(object: I): ReviewRecord {
    const message = createBaseReviewRecord()
    message.recordHash = object.recordHash ?? ''
    message.actionNumber = object.actionNumber ?? ''
    message.recordResult = object.recordResult ?? ''
    message.reviewedAddress = object.reviewedAddress ?? ''
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
