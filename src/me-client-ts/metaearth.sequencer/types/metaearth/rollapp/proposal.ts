// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/rollapp/proposal.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.rollapp'

export interface SubmitFraudProposal {
  title: string
  description: string
  /** The rollapp id */
  rollappId: string
  /** The ibc client id of the rollapp */
  ibcClientId: string
  /** The height of the fraudelent block */
  fraudelentHeight: number
  /** The address of the fraudelent sequencer */
  fraudelentSequencerAddress: string
}

function createBaseSubmitFraudProposal(): SubmitFraudProposal {
  return {
    title: '',
    description: '',
    rollappId: '',
    ibcClientId: '',
    fraudelentHeight: 0,
    fraudelentSequencerAddress: '',
  }
}

export const SubmitFraudProposal: MessageFns<SubmitFraudProposal> = {
  encode(message: SubmitFraudProposal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== '') {
      writer.uint32(10).string(message.title)
    }
    if (message.description !== '') {
      writer.uint32(18).string(message.description)
    }
    if (message.rollappId !== '') {
      writer.uint32(26).string(message.rollappId)
    }
    if (message.ibcClientId !== '') {
      writer.uint32(34).string(message.ibcClientId)
    }
    if (message.fraudelentHeight !== 0) {
      writer.uint32(40).uint64(message.fraudelentHeight)
    }
    if (message.fraudelentSequencerAddress !== '') {
      writer.uint32(50).string(message.fraudelentSequencerAddress)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitFraudProposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseSubmitFraudProposal()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.title = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.description = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.rollappId = reader.string()
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.ibcClientId = reader.string()
          continue
        }
        case 5: {
          if (tag !== 40) {
            break
          }

          message.fraudelentHeight = longToNumber(reader.uint64())
          continue
        }
        case 6: {
          if (tag !== 50) {
            break
          }

          message.fraudelentSequencerAddress = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): SubmitFraudProposal {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
          ? globalThis.String(object.rollapp_id)
          : '',
      ibcClientId: isSet(object.ibcClientId)
        ? globalThis.String(object.ibcClientId)
        : isSet(object.ibc_client_id)
          ? globalThis.String(object.ibc_client_id)
          : '',
      fraudelentHeight: isSet(object.fraudelentHeight)
        ? globalThis.Number(object.fraudelentHeight)
        : isSet(object.fraudelent_height)
          ? globalThis.Number(object.fraudelent_height)
          : 0,
      fraudelentSequencerAddress: isSet(object.fraudelentSequencerAddress)
        ? globalThis.String(object.fraudelentSequencerAddress)
        : isSet(object.fraudelent_sequencer_address)
          ? globalThis.String(object.fraudelent_sequencer_address)
          : '',
    }
  },

  toJSON(message: SubmitFraudProposal): unknown {
    const obj: any = {}
    if (message.title !== '') {
      obj.title = message.title
    }
    if (message.description !== '') {
      obj.description = message.description
    }
    if (message.rollappId !== '') {
      obj.rollappId = message.rollappId
    }
    if (message.ibcClientId !== '') {
      obj.ibcClientId = message.ibcClientId
    }
    if (message.fraudelentHeight !== 0) {
      obj.fraudelentHeight = Math.round(message.fraudelentHeight)
    }
    if (message.fraudelentSequencerAddress !== '') {
      obj.fraudelentSequencerAddress = message.fraudelentSequencerAddress
    }
    return obj
  },

  create<I extends Exact<DeepPartial<SubmitFraudProposal>, I>>(base?: I): SubmitFraudProposal {
    return SubmitFraudProposal.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<SubmitFraudProposal>, I>>(
    object: I,
  ): SubmitFraudProposal {
    const message = createBaseSubmitFraudProposal()
    message.title = object.title ?? ''
    message.description = object.description ?? ''
    message.rollappId = object.rollappId ?? ''
    message.ibcClientId = object.ibcClientId ?? ''
    message.fraudelentHeight = object.fraudelentHeight ?? 0
    message.fraudelentSequencerAddress = object.fraudelentSequencerAddress ?? ''
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
