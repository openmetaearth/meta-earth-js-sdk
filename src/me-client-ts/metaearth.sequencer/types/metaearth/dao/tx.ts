// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/dao/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { DaoAddresses } from './dao'

export const protobufPackage = 'metaearth.dao'

export interface MsgUpdateDao {
  creator: string
  daoAddresses: DaoAddresses | undefined
}

export interface MsgUpdateDaoResponse {}

export interface MsgFreeGasAccount {
  creator: string
  accounts: FreeGasAccount[]
}

export interface FreeGasAccount {
  address: string
  isFree: boolean
}

export interface MsgFreeGasAccountResponse {}

function createBaseMsgUpdateDao(): MsgUpdateDao {
  return { creator: '', daoAddresses: undefined }
}

export const MsgUpdateDao: MessageFns<MsgUpdateDao> = {
  encode(message: MsgUpdateDao, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== '') {
      writer.uint32(10).string(message.creator)
    }
    if (message.daoAddresses !== undefined) {
      DaoAddresses.encode(message.daoAddresses, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateDao {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMsgUpdateDao()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.creator = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.daoAddresses = DaoAddresses.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): MsgUpdateDao {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : '',
      daoAddresses: isSet(object.daoAddresses)
        ? DaoAddresses.fromJSON(object.daoAddresses)
        : isSet(object.dao_addresses)
          ? DaoAddresses.fromJSON(object.dao_addresses)
          : undefined,
    }
  },

  toJSON(message: MsgUpdateDao): unknown {
    const obj: any = {}
    if (message.creator !== '') {
      obj.creator = message.creator
    }
    if (message.daoAddresses !== undefined) {
      obj.daoAddresses = DaoAddresses.toJSON(message.daoAddresses)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<MsgUpdateDao>, I>>(base?: I): MsgUpdateDao {
    return MsgUpdateDao.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateDao>, I>>(object: I): MsgUpdateDao {
    const message = createBaseMsgUpdateDao()
    message.creator = object.creator ?? ''
    message.daoAddresses =
      object.daoAddresses !== undefined && object.daoAddresses !== null
        ? DaoAddresses.fromPartial(object.daoAddresses)
        : undefined
    return message
  },
}

function createBaseMsgUpdateDaoResponse(): MsgUpdateDaoResponse {
  return {}
}

export const MsgUpdateDaoResponse: MessageFns<MsgUpdateDaoResponse> = {
  encode(_: MsgUpdateDaoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateDaoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMsgUpdateDaoResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(_: any): MsgUpdateDaoResponse {
    return {}
  },

  toJSON(_: MsgUpdateDaoResponse): unknown {
    const obj: any = {}
    return obj
  },

  create<I extends Exact<DeepPartial<MsgUpdateDaoResponse>, I>>(base?: I): MsgUpdateDaoResponse {
    return MsgUpdateDaoResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateDaoResponse>, I>>(_: I): MsgUpdateDaoResponse {
    const message = createBaseMsgUpdateDaoResponse()
    return message
  },
}

function createBaseMsgFreeGasAccount(): MsgFreeGasAccount {
  return { creator: '', accounts: [] }
}

export const MsgFreeGasAccount: MessageFns<MsgFreeGasAccount> = {
  encode(message: MsgFreeGasAccount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== '') {
      writer.uint32(10).string(message.creator)
    }
    for (const v of message.accounts) {
      FreeGasAccount.encode(v!, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgFreeGasAccount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMsgFreeGasAccount()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.creator = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.accounts.push(FreeGasAccount.decode(reader, reader.uint32()))
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): MsgFreeGasAccount {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : '',
      accounts: globalThis.Array.isArray(object?.accounts)
        ? object.accounts.map((e: any) => FreeGasAccount.fromJSON(e))
        : [],
    }
  },

  toJSON(message: MsgFreeGasAccount): unknown {
    const obj: any = {}
    if (message.creator !== '') {
      obj.creator = message.creator
    }
    if (message.accounts?.length) {
      obj.accounts = message.accounts.map((e) => FreeGasAccount.toJSON(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<MsgFreeGasAccount>, I>>(base?: I): MsgFreeGasAccount {
    return MsgFreeGasAccount.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<MsgFreeGasAccount>, I>>(object: I): MsgFreeGasAccount {
    const message = createBaseMsgFreeGasAccount()
    message.creator = object.creator ?? ''
    message.accounts = object.accounts?.map((e) => FreeGasAccount.fromPartial(e)) || []
    return message
  },
}

function createBaseFreeGasAccount(): FreeGasAccount {
  return { address: '', isFree: false }
}

export const FreeGasAccount: MessageFns<FreeGasAccount> = {
  encode(message: FreeGasAccount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== '') {
      writer.uint32(10).string(message.address)
    }
    if (message.isFree !== false) {
      writer.uint32(16).bool(message.isFree)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FreeGasAccount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseFreeGasAccount()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.address = reader.string()
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.isFree = reader.bool()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): FreeGasAccount {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : '',
      isFree: isSet(object.isFree)
        ? globalThis.Boolean(object.isFree)
        : isSet(object.is_free)
          ? globalThis.Boolean(object.is_free)
          : false,
    }
  },

  toJSON(message: FreeGasAccount): unknown {
    const obj: any = {}
    if (message.address !== '') {
      obj.address = message.address
    }
    if (message.isFree !== false) {
      obj.isFree = message.isFree
    }
    return obj
  },

  create<I extends Exact<DeepPartial<FreeGasAccount>, I>>(base?: I): FreeGasAccount {
    return FreeGasAccount.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<FreeGasAccount>, I>>(object: I): FreeGasAccount {
    const message = createBaseFreeGasAccount()
    message.address = object.address ?? ''
    message.isFree = object.isFree ?? false
    return message
  },
}

function createBaseMsgFreeGasAccountResponse(): MsgFreeGasAccountResponse {
  return {}
}

export const MsgFreeGasAccountResponse: MessageFns<MsgFreeGasAccountResponse> = {
  encode(_: MsgFreeGasAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgFreeGasAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMsgFreeGasAccountResponse()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(_: any): MsgFreeGasAccountResponse {
    return {}
  },

  toJSON(_: MsgFreeGasAccountResponse): unknown {
    const obj: any = {}
    return obj
  },

  create<I extends Exact<DeepPartial<MsgFreeGasAccountResponse>, I>>(
    base?: I,
  ): MsgFreeGasAccountResponse {
    return MsgFreeGasAccountResponse.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<MsgFreeGasAccountResponse>, I>>(
    _: I,
  ): MsgFreeGasAccountResponse {
    const message = createBaseMsgFreeGasAccountResponse()
    return message
  },
}

/** Msg defines the Msg service. */
export interface Msg {
  UpdateDao(request: MsgUpdateDao): Promise<MsgUpdateDaoResponse>
  FreeGasAccount(request: MsgFreeGasAccount): Promise<MsgFreeGasAccountResponse>
}

export const MsgServiceName = 'metaearth.dao.Msg'
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc
  private readonly service: string
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName
    this.rpc = rpc
    this.UpdateDao = this.UpdateDao.bind(this)
    this.FreeGasAccount = this.FreeGasAccount.bind(this)
  }
  UpdateDao(request: MsgUpdateDao): Promise<MsgUpdateDaoResponse> {
    const data = MsgUpdateDao.encode(request).finish()
    const promise = this.rpc.request(this.service, 'UpdateDao', data)
    return promise.then((data) => MsgUpdateDaoResponse.decode(new BinaryReader(data)))
  }

  FreeGasAccount(request: MsgFreeGasAccount): Promise<MsgFreeGasAccountResponse> {
    const data = MsgFreeGasAccount.encode(request).finish()
    const promise = this.rpc.request(this.service, 'FreeGasAccount', data)
    return promise.then((data) => MsgFreeGasAccountResponse.decode(new BinaryReader(data)))
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
