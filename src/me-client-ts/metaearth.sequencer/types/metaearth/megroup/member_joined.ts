// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/megroup/member_joined.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.megroup'

export interface MemberJoined {
  address: string
  groupId: number
}

function createBaseMemberJoined(): MemberJoined {
  return { address: '', groupId: 0 }
}

export const MemberJoined: MessageFns<MemberJoined> = {
  encode(message: MemberJoined, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== '') {
      writer.uint32(10).string(message.address)
    }
    if (message.groupId !== 0) {
      writer.uint32(16).uint64(message.groupId)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MemberJoined {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseMemberJoined()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.address = reader.string()
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.groupId = longToNumber(reader.uint64())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): MemberJoined {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : '',
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
    }
  },

  toJSON(message: MemberJoined): unknown {
    const obj: any = {}
    if (message.address !== '') {
      obj.address = message.address
    }
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<MemberJoined>, I>>(base?: I): MemberJoined {
    return MemberJoined.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<MemberJoined>, I>>(object: I): MemberJoined {
    const message = createBaseMemberJoined()
    message.address = object.address ?? ''
    message.groupId = object.groupId ?? 0
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
