// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/eibc/demand_order.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Coin } from '../../cosmos/base/v1beta1/coin'
import {
  RollappPacket_Type,
  rollappPacket_TypeFromJSON,
  rollappPacket_TypeToJSON,
} from '../common/rollapp_packet'
import { Status, statusFromJSON, statusToJSON } from '../common/status'

export const protobufPackage = 'metaearth.eibc'

export interface DemandOrder {
  /**
   * id is a hash of the form generated by GetRollappPacketKey,
   * e.g
   * status/rollappid/packetProofHeight/packetDestinationChannel-PacketSequence
   * which guarantees uniqueness
   */
  id: string
  /**
   * tracking_packet_key is the key of the packet that is being tracked.
   * This key can change depends on the packet status.
   */
  trackingPacketKey: string
  /**
   * price is the amount that the fulfiller sends to original eibc transfer
   * recipient
   */
  price: Coin[]
  /**
   * fee is the effective profit made by the fulfiller because they pay price
   * and receive fee + price
   */
  fee: Coin[]
  recipient: string
  trackingPacketStatus: Status
  rollappId: string
  type: RollappPacket_Type
  /**
   * fulfiller_address is the bech32-encoded address of the account which
   * fulfilled the order.
   */
  fulfillerAddress: string
}

function createBaseDemandOrder(): DemandOrder {
  return {
    id: '',
    trackingPacketKey: '',
    price: [],
    fee: [],
    recipient: '',
    trackingPacketStatus: 0,
    rollappId: '',
    type: 0,
    fulfillerAddress: '',
  }
}

export const DemandOrder: MessageFns<DemandOrder> = {
  encode(message: DemandOrder, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id)
    }
    if (message.trackingPacketKey !== '') {
      writer.uint32(18).string(message.trackingPacketKey)
    }
    for (const v of message.price) {
      Coin.encode(v!, writer.uint32(26).fork()).join()
    }
    for (const v of message.fee) {
      Coin.encode(v!, writer.uint32(34).fork()).join()
    }
    if (message.recipient !== '') {
      writer.uint32(42).string(message.recipient)
    }
    if (message.trackingPacketStatus !== 0) {
      writer.uint32(64).int32(message.trackingPacketStatus)
    }
    if (message.rollappId !== '') {
      writer.uint32(74).string(message.rollappId)
    }
    if (message.type !== 0) {
      writer.uint32(80).int32(message.type)
    }
    if (message.fulfillerAddress !== '') {
      writer.uint32(90).string(message.fulfillerAddress)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DemandOrder {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseDemandOrder()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.id = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.trackingPacketKey = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.price.push(Coin.decode(reader, reader.uint32()))
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.fee.push(Coin.decode(reader, reader.uint32()))
          continue
        }
        case 5: {
          if (tag !== 42) {
            break
          }

          message.recipient = reader.string()
          continue
        }
        case 8: {
          if (tag !== 64) {
            break
          }

          message.trackingPacketStatus = reader.int32() as any
          continue
        }
        case 9: {
          if (tag !== 74) {
            break
          }

          message.rollappId = reader.string()
          continue
        }
        case 10: {
          if (tag !== 80) {
            break
          }

          message.type = reader.int32() as any
          continue
        }
        case 11: {
          if (tag !== 90) {
            break
          }

          message.fulfillerAddress = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): DemandOrder {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      trackingPacketKey: isSet(object.trackingPacketKey)
        ? globalThis.String(object.trackingPacketKey)
        : isSet(object.tracking_packet_key)
          ? globalThis.String(object.tracking_packet_key)
          : '',
      price: globalThis.Array.isArray(object?.price)
        ? object.price.map((e: any) => Coin.fromJSON(e))
        : [],
      fee: globalThis.Array.isArray(object?.fee)
        ? object.fee.map((e: any) => Coin.fromJSON(e))
        : [],
      recipient: isSet(object.recipient) ? globalThis.String(object.recipient) : '',
      trackingPacketStatus: isSet(object.trackingPacketStatus)
        ? statusFromJSON(object.trackingPacketStatus)
        : isSet(object.tracking_packet_status)
          ? statusFromJSON(object.tracking_packet_status)
          : 0,
      rollappId: isSet(object.rollappId)
        ? globalThis.String(object.rollappId)
        : isSet(object.rollapp_id)
          ? globalThis.String(object.rollapp_id)
          : '',
      type: isSet(object.type) ? rollappPacket_TypeFromJSON(object.type) : 0,
      fulfillerAddress: isSet(object.fulfillerAddress)
        ? globalThis.String(object.fulfillerAddress)
        : isSet(object.fulfiller_address)
          ? globalThis.String(object.fulfiller_address)
          : '',
    }
  },

  toJSON(message: DemandOrder): unknown {
    const obj: any = {}
    if (message.id !== '') {
      obj.id = message.id
    }
    if (message.trackingPacketKey !== '') {
      obj.trackingPacketKey = message.trackingPacketKey
    }
    if (message.price?.length) {
      obj.price = message.price.map((e) => Coin.toJSON(e))
    }
    if (message.fee?.length) {
      obj.fee = message.fee.map((e) => Coin.toJSON(e))
    }
    if (message.recipient !== '') {
      obj.recipient = message.recipient
    }
    if (message.trackingPacketStatus !== 0) {
      obj.trackingPacketStatus = statusToJSON(message.trackingPacketStatus)
    }
    if (message.rollappId !== '') {
      obj.rollappId = message.rollappId
    }
    if (message.type !== 0) {
      obj.type = rollappPacket_TypeToJSON(message.type)
    }
    if (message.fulfillerAddress !== '') {
      obj.fulfillerAddress = message.fulfillerAddress
    }
    return obj
  },

  create<I extends Exact<DeepPartial<DemandOrder>, I>>(base?: I): DemandOrder {
    return DemandOrder.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<DemandOrder>, I>>(object: I): DemandOrder {
    const message = createBaseDemandOrder()
    message.id = object.id ?? ''
    message.trackingPacketKey = object.trackingPacketKey ?? ''
    message.price = object.price?.map((e) => Coin.fromPartial(e)) || []
    message.fee = object.fee?.map((e) => Coin.fromPartial(e)) || []
    message.recipient = object.recipient ?? ''
    message.trackingPacketStatus = object.trackingPacketStatus ?? 0
    message.rollappId = object.rollappId ?? ''
    message.type = object.type ?? 0
    message.fulfillerAddress = object.fulfillerAddress ?? ''
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
