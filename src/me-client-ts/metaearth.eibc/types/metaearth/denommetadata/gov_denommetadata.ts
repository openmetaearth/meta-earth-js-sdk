// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/denommetadata/gov_denommetadata.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Metadata } from '../../cosmos/bank/v1beta1/bank'

export const protobufPackage = 'metaearth.denommetadata'

export interface CreateDenomMetadataProposal {
  title: string
  description: string
  tokenMetadata: Metadata[]
}

export interface UpdateDenomMetadataProposal {
  title: string
  description: string
  tokenMetadata: Metadata[]
}

function createBaseCreateDenomMetadataProposal(): CreateDenomMetadataProposal {
  return { title: '', description: '', tokenMetadata: [] }
}

export const CreateDenomMetadataProposal: MessageFns<CreateDenomMetadataProposal> = {
  encode(
    message: CreateDenomMetadataProposal,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.title !== '') {
      writer.uint32(10).string(message.title)
    }
    if (message.description !== '') {
      writer.uint32(18).string(message.description)
    }
    for (const v of message.tokenMetadata) {
      Metadata.encode(v!, writer.uint32(26).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateDenomMetadataProposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseCreateDenomMetadataProposal()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.title = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.description = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.tokenMetadata.push(Metadata.decode(reader, reader.uint32()))
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): CreateDenomMetadataProposal {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
      tokenMetadata: globalThis.Array.isArray(object?.tokenMetadata)
        ? object.tokenMetadata.map((e: any) => Metadata.fromJSON(e))
        : [],
    }
  },

  toJSON(message: CreateDenomMetadataProposal): unknown {
    const obj: any = {}
    if (message.title !== '') {
      obj.title = message.title
    }
    if (message.description !== '') {
      obj.description = message.description
    }
    if (message.tokenMetadata?.length) {
      obj.tokenMetadata = message.tokenMetadata.map((e) => Metadata.toJSON(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<CreateDenomMetadataProposal>, I>>(
    base?: I,
  ): CreateDenomMetadataProposal {
    return CreateDenomMetadataProposal.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<CreateDenomMetadataProposal>, I>>(
    object: I,
  ): CreateDenomMetadataProposal {
    const message = createBaseCreateDenomMetadataProposal()
    message.title = object.title ?? ''
    message.description = object.description ?? ''
    message.tokenMetadata = object.tokenMetadata?.map((e) => Metadata.fromPartial(e)) || []
    return message
  },
}

function createBaseUpdateDenomMetadataProposal(): UpdateDenomMetadataProposal {
  return { title: '', description: '', tokenMetadata: [] }
}

export const UpdateDenomMetadataProposal: MessageFns<UpdateDenomMetadataProposal> = {
  encode(
    message: UpdateDenomMetadataProposal,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.title !== '') {
      writer.uint32(10).string(message.title)
    }
    if (message.description !== '') {
      writer.uint32(18).string(message.description)
    }
    for (const v of message.tokenMetadata) {
      Metadata.encode(v!, writer.uint32(26).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateDenomMetadataProposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseUpdateDenomMetadataProposal()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.title = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.description = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.tokenMetadata.push(Metadata.decode(reader, reader.uint32()))
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): UpdateDenomMetadataProposal {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : '',
      tokenMetadata: globalThis.Array.isArray(object?.tokenMetadata)
        ? object.tokenMetadata.map((e: any) => Metadata.fromJSON(e))
        : [],
    }
  },

  toJSON(message: UpdateDenomMetadataProposal): unknown {
    const obj: any = {}
    if (message.title !== '') {
      obj.title = message.title
    }
    if (message.description !== '') {
      obj.description = message.description
    }
    if (message.tokenMetadata?.length) {
      obj.tokenMetadata = message.tokenMetadata.map((e) => Metadata.toJSON(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<UpdateDenomMetadataProposal>, I>>(
    base?: I,
  ): UpdateDenomMetadataProposal {
    return UpdateDenomMetadataProposal.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<UpdateDenomMetadataProposal>, I>>(
    object: I,
  ): UpdateDenomMetadataProposal {
    const message = createBaseUpdateDenomMetadataProposal()
    message.title = object.title ?? ''
    message.description = object.description ?? ''
    message.tokenMetadata = object.tokenMetadata?.map((e) => Metadata.fromPartial(e)) || []
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
