// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/rollapp/genesis_transfer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.rollapp'

/**
 * Bookkeeping for the genesis transfer bridge protocol.
 * Each rollapp will have one of these items corresponding to it.
 */
export interface GenesisTransfers {
  rollappID: string
  /**
   * The total number of incoming ibc transfers to be fast tracked in the
   * genesis transfer period
   */
  numTotal: number
  /**
   * The number of transfers already processed, when this number reaches
   * numTotal the genesis transfer window closes.
   */
  numReceived: number
}

function createBaseGenesisTransfers(): GenesisTransfers {
  return { rollappID: '', numTotal: 0, numReceived: 0 }
}

export const GenesisTransfers: MessageFns<GenesisTransfers> = {
  encode(message: GenesisTransfers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollappID !== '') {
      writer.uint32(10).string(message.rollappID)
    }
    if (message.numTotal !== 0) {
      writer.uint32(16).uint64(message.numTotal)
    }
    if (message.numReceived !== 0) {
      writer.uint32(24).uint64(message.numReceived)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisTransfers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseGenesisTransfers()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.rollappID = reader.string()
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.numTotal = longToNumber(reader.uint64())
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.numReceived = longToNumber(reader.uint64())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): GenesisTransfers {
    return {
      rollappID: isSet(object.rollappID) ? globalThis.String(object.rollappID) : '',
      numTotal: isSet(object.numTotal) ? globalThis.Number(object.numTotal) : 0,
      numReceived: isSet(object.numReceived) ? globalThis.Number(object.numReceived) : 0,
    }
  },

  toJSON(message: GenesisTransfers): unknown {
    const obj: any = {}
    if (message.rollappID !== '') {
      obj.rollappID = message.rollappID
    }
    if (message.numTotal !== 0) {
      obj.numTotal = Math.round(message.numTotal)
    }
    if (message.numReceived !== 0) {
      obj.numReceived = Math.round(message.numReceived)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<GenesisTransfers>, I>>(base?: I): GenesisTransfers {
    return GenesisTransfers.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<GenesisTransfers>, I>>(object: I): GenesisTransfers {
    const message = createBaseGenesisTransfers()
    message.rollappID = object.rollappID ?? ''
    message.numTotal = object.numTotal ?? 0
    message.numReceived = object.numReceived ?? 0
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
