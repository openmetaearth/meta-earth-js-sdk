// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/rollapp/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.rollapp'

export interface DeployerParams {
  /**
   * address is a bech32-encoded address of the
   * accounts that are allowed to create a rollapp.
   */
  address: string
}

/** Params defines the parameters for the module. */
export interface Params {
  /**
   * dispute_period_in_blocks the number of blocks it takes
   * to change a status of a state from received to finalized.
   * during that period, any user could submit fraud proof
   */
  disputePeriodInBlocks: number
  /**
   * deployer_whitelist is a list of the
   * accounts that are allowed to create a rollapp and maximum number of
   * rollapps. In the case of an empty list, there are no restrictions
   */
  deployerWhitelist: DeployerParams[]
  rollappsEnabled: boolean
}

function createBaseDeployerParams(): DeployerParams {
  return { address: '' }
}

export const DeployerParams: MessageFns<DeployerParams> = {
  encode(message: DeployerParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== '') {
      writer.uint32(10).string(message.address)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeployerParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseDeployerParams()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.address = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): DeployerParams {
    return { address: isSet(object.address) ? globalThis.String(object.address) : '' }
  },

  toJSON(message: DeployerParams): unknown {
    const obj: any = {}
    if (message.address !== '') {
      obj.address = message.address
    }
    return obj
  },

  create<I extends Exact<DeepPartial<DeployerParams>, I>>(base?: I): DeployerParams {
    return DeployerParams.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<DeployerParams>, I>>(object: I): DeployerParams {
    const message = createBaseDeployerParams()
    message.address = object.address ?? ''
    return message
  },
}

function createBaseParams(): Params {
  return { disputePeriodInBlocks: 0, deployerWhitelist: [], rollappsEnabled: false }
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.disputePeriodInBlocks !== 0) {
      writer.uint32(8).uint64(message.disputePeriodInBlocks)
    }
    for (const v of message.deployerWhitelist) {
      DeployerParams.encode(v!, writer.uint32(18).fork()).join()
    }
    if (message.rollappsEnabled !== false) {
      writer.uint32(24).bool(message.rollappsEnabled)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseParams()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.disputePeriodInBlocks = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.deployerWhitelist.push(DeployerParams.decode(reader, reader.uint32()))
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.rollappsEnabled = reader.bool()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): Params {
    return {
      disputePeriodInBlocks: isSet(object.disputePeriodInBlocks)
        ? globalThis.Number(object.disputePeriodInBlocks)
        : isSet(object.dispute_period_in_blocks)
          ? globalThis.Number(object.dispute_period_in_blocks)
          : 0,
      deployerWhitelist: globalThis.Array.isArray(object?.deployerWhitelist)
        ? object.deployerWhitelist.map((e: any) => DeployerParams.fromJSON(e))
        : globalThis.Array.isArray(object?.deployer_whitelist)
          ? object.deployer_whitelist.map((e: any) => DeployerParams.fromJSON(e))
          : [],
      rollappsEnabled: isSet(object.rollappsEnabled)
        ? globalThis.Boolean(object.rollappsEnabled)
        : isSet(object.rollapps_enabled)
          ? globalThis.Boolean(object.rollapps_enabled)
          : false,
    }
  },

  toJSON(message: Params): unknown {
    const obj: any = {}
    if (message.disputePeriodInBlocks !== 0) {
      obj.disputePeriodInBlocks = Math.round(message.disputePeriodInBlocks)
    }
    if (message.deployerWhitelist?.length) {
      obj.deployerWhitelist = message.deployerWhitelist.map((e) => DeployerParams.toJSON(e))
    }
    if (message.rollappsEnabled !== false) {
      obj.rollappsEnabled = message.rollappsEnabled
    }
    return obj
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams()
    message.disputePeriodInBlocks = object.disputePeriodInBlocks ?? 0
    message.deployerWhitelist =
      object.deployerWhitelist?.map((e) => DeployerParams.fromPartial(e)) || []
    message.rollappsEnabled = object.rollappsEnabled ?? false
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
