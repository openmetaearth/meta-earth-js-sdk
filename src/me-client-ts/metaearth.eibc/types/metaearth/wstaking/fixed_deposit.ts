// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/wstaking/fixed_deposit.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'
import { Coin } from '../../cosmos/base/v1beta1/coin'
import { Timestamp } from '../../google/protobuf/timestamp'

export const protobufPackage = 'metaearth.wstaking'

export enum FixedDepositState {
  ALL_STATE = 0,
  NOT_EXPIRED = 1,
  EXPIRED = 2,
  UNRECOGNIZED = -1,
}

export function fixedDepositStateFromJSON(object: any): FixedDepositState {
  switch (object) {
    case 0:
    case 'ALL_STATE':
      return FixedDepositState.ALL_STATE
    case 1:
    case 'NOT_EXPIRED':
      return FixedDepositState.NOT_EXPIRED
    case 2:
    case 'EXPIRED':
      return FixedDepositState.EXPIRED
    case -1:
    case 'UNRECOGNIZED':
    default:
      return FixedDepositState.UNRECOGNIZED
  }
}

export function fixedDepositStateToJSON(object: FixedDepositState): string {
  switch (object) {
    case FixedDepositState.ALL_STATE:
      return 'ALL_STATE'
    case FixedDepositState.NOT_EXPIRED:
      return 'NOT_EXPIRED'
    case FixedDepositState.EXPIRED:
      return 'EXPIRED'
    case FixedDepositState.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED'
  }
}

export enum fixedDepositCfgStatus {
  FIXED_DEPOSIT_CFG_ACTIVE = 0,
  FIXED_DEPOSIT_CFG_INACTIVE = 1,
  UNRECOGNIZED = -1,
}

export function fixedDepositCfgStatusFromJSON(object: any): fixedDepositCfgStatus {
  switch (object) {
    case 0:
    case 'FIXED_DEPOSIT_CFG_ACTIVE':
      return fixedDepositCfgStatus.FIXED_DEPOSIT_CFG_ACTIVE
    case 1:
    case 'FIXED_DEPOSIT_CFG_INACTIVE':
      return fixedDepositCfgStatus.FIXED_DEPOSIT_CFG_INACTIVE
    case -1:
    case 'UNRECOGNIZED':
    default:
      return fixedDepositCfgStatus.UNRECOGNIZED
  }
}

export function fixedDepositCfgStatusToJSON(object: fixedDepositCfgStatus): string {
  switch (object) {
    case fixedDepositCfgStatus.FIXED_DEPOSIT_CFG_ACTIVE:
      return 'FIXED_DEPOSIT_CFG_ACTIVE'
    case fixedDepositCfgStatus.FIXED_DEPOSIT_CFG_INACTIVE:
      return 'FIXED_DEPOSIT_CFG_INACTIVE'
    case fixedDepositCfgStatus.UNRECOGNIZED:
    default:
      return 'UNRECOGNIZED'
  }
}

export interface FixedDeposit {
  id: number
  account: string
  principal: Coin | undefined
  interest: Coin | undefined
  startTime: Date | undefined
  endTime: Date | undefined
  term: number
  rate: string
}

export interface FixedDepositTotal {
  Amount: Coin | undefined
}

export interface FixedDepositCfg {
  regionId: string
  term: number
  rate: string
  status: fixedDepositCfgStatus
}

export interface RegionAllFixedDepositCfg {
  regionId: string
  RegionFixedDepositCfg: RegionFixedDepositCfg[]
}

export interface RegionFixedDepositCfg {
  term: number
  rate: string
  status: fixedDepositCfgStatus
}

function createBaseFixedDeposit(): FixedDeposit {
  return {
    id: 0,
    account: '',
    principal: undefined,
    interest: undefined,
    startTime: undefined,
    endTime: undefined,
    term: 0,
    rate: '',
  }
}

export const FixedDeposit: MessageFns<FixedDeposit> = {
  encode(message: FixedDeposit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id)
    }
    if (message.account !== '') {
      writer.uint32(18).string(message.account)
    }
    if (message.principal !== undefined) {
      Coin.encode(message.principal, writer.uint32(26).fork()).join()
    }
    if (message.interest !== undefined) {
      Coin.encode(message.interest, writer.uint32(34).fork()).join()
    }
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(42).fork()).join()
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(50).fork()).join()
    }
    if (message.term !== 0) {
      writer.uint32(56).int64(message.term)
    }
    if (message.rate !== '') {
      writer.uint32(66).string(message.rate)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FixedDeposit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseFixedDeposit()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.id = longToNumber(reader.uint64())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.account = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.principal = Coin.decode(reader, reader.uint32())
          continue
        }
        case 4: {
          if (tag !== 34) {
            break
          }

          message.interest = Coin.decode(reader, reader.uint32())
          continue
        }
        case 5: {
          if (tag !== 42) {
            break
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()))
          continue
        }
        case 6: {
          if (tag !== 50) {
            break
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()))
          continue
        }
        case 7: {
          if (tag !== 56) {
            break
          }

          message.term = longToNumber(reader.int64())
          continue
        }
        case 8: {
          if (tag !== 66) {
            break
          }

          message.rate = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): FixedDeposit {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      account: isSet(object.account) ? globalThis.String(object.account) : '',
      principal: isSet(object.principal) ? Coin.fromJSON(object.principal) : undefined,
      interest: isSet(object.interest) ? Coin.fromJSON(object.interest) : undefined,
      startTime: isSet(object.startTime)
        ? fromJsonTimestamp(object.startTime)
        : isSet(object.start_time)
          ? fromJsonTimestamp(object.start_time)
          : undefined,
      endTime: isSet(object.endTime)
        ? fromJsonTimestamp(object.endTime)
        : isSet(object.end_time)
          ? fromJsonTimestamp(object.end_time)
          : undefined,
      term: isSet(object.term) ? globalThis.Number(object.term) : 0,
      rate: isSet(object.rate) ? globalThis.String(object.rate) : '',
    }
  },

  toJSON(message: FixedDeposit): unknown {
    const obj: any = {}
    if (message.id !== 0) {
      obj.id = Math.round(message.id)
    }
    if (message.account !== '') {
      obj.account = message.account
    }
    if (message.principal !== undefined) {
      obj.principal = Coin.toJSON(message.principal)
    }
    if (message.interest !== undefined) {
      obj.interest = Coin.toJSON(message.interest)
    }
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString()
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString()
    }
    if (message.term !== 0) {
      obj.term = Math.round(message.term)
    }
    if (message.rate !== '') {
      obj.rate = message.rate
    }
    return obj
  },

  create<I extends Exact<DeepPartial<FixedDeposit>, I>>(base?: I): FixedDeposit {
    return FixedDeposit.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<FixedDeposit>, I>>(object: I): FixedDeposit {
    const message = createBaseFixedDeposit()
    message.id = object.id ?? 0
    message.account = object.account ?? ''
    message.principal =
      object.principal !== undefined && object.principal !== null
        ? Coin.fromPartial(object.principal)
        : undefined
    message.interest =
      object.interest !== undefined && object.interest !== null
        ? Coin.fromPartial(object.interest)
        : undefined
    message.startTime = object.startTime ?? undefined
    message.endTime = object.endTime ?? undefined
    message.term = object.term ?? 0
    message.rate = object.rate ?? ''
    return message
  },
}

function createBaseFixedDepositTotal(): FixedDepositTotal {
  return { Amount: undefined }
}

export const FixedDepositTotal: MessageFns<FixedDepositTotal> = {
  encode(message: FixedDepositTotal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.Amount !== undefined) {
      Coin.encode(message.Amount, writer.uint32(10).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FixedDepositTotal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseFixedDepositTotal()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.Amount = Coin.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): FixedDepositTotal {
    return { Amount: isSet(object.Amount) ? Coin.fromJSON(object.Amount) : undefined }
  },

  toJSON(message: FixedDepositTotal): unknown {
    const obj: any = {}
    if (message.Amount !== undefined) {
      obj.Amount = Coin.toJSON(message.Amount)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<FixedDepositTotal>, I>>(base?: I): FixedDepositTotal {
    return FixedDepositTotal.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<FixedDepositTotal>, I>>(object: I): FixedDepositTotal {
    const message = createBaseFixedDepositTotal()
    message.Amount =
      object.Amount !== undefined && object.Amount !== null
        ? Coin.fromPartial(object.Amount)
        : undefined
    return message
  },
}

function createBaseFixedDepositCfg(): FixedDepositCfg {
  return { regionId: '', term: 0, rate: '', status: 0 }
}

export const FixedDepositCfg: MessageFns<FixedDepositCfg> = {
  encode(message: FixedDepositCfg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.regionId !== '') {
      writer.uint32(10).string(message.regionId)
    }
    if (message.term !== 0) {
      writer.uint32(16).int64(message.term)
    }
    if (message.rate !== '') {
      writer.uint32(26).string(message.rate)
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FixedDepositCfg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseFixedDepositCfg()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.regionId = reader.string()
          continue
        }
        case 2: {
          if (tag !== 16) {
            break
          }

          message.term = longToNumber(reader.int64())
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.rate = reader.string()
          continue
        }
        case 4: {
          if (tag !== 32) {
            break
          }

          message.status = reader.int32() as any
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): FixedDepositCfg {
    return {
      regionId: isSet(object.regionId) ? globalThis.String(object.regionId) : '',
      term: isSet(object.term) ? globalThis.Number(object.term) : 0,
      rate: isSet(object.rate) ? globalThis.String(object.rate) : '',
      status: isSet(object.status) ? fixedDepositCfgStatusFromJSON(object.status) : 0,
    }
  },

  toJSON(message: FixedDepositCfg): unknown {
    const obj: any = {}
    if (message.regionId !== '') {
      obj.regionId = message.regionId
    }
    if (message.term !== 0) {
      obj.term = Math.round(message.term)
    }
    if (message.rate !== '') {
      obj.rate = message.rate
    }
    if (message.status !== 0) {
      obj.status = fixedDepositCfgStatusToJSON(message.status)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<FixedDepositCfg>, I>>(base?: I): FixedDepositCfg {
    return FixedDepositCfg.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<FixedDepositCfg>, I>>(object: I): FixedDepositCfg {
    const message = createBaseFixedDepositCfg()
    message.regionId = object.regionId ?? ''
    message.term = object.term ?? 0
    message.rate = object.rate ?? ''
    message.status = object.status ?? 0
    return message
  },
}

function createBaseRegionAllFixedDepositCfg(): RegionAllFixedDepositCfg {
  return { regionId: '', RegionFixedDepositCfg: [] }
}

export const RegionAllFixedDepositCfg: MessageFns<RegionAllFixedDepositCfg> = {
  encode(
    message: RegionAllFixedDepositCfg,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.regionId !== '') {
      writer.uint32(10).string(message.regionId)
    }
    for (const v of message.RegionFixedDepositCfg) {
      RegionFixedDepositCfg.encode(v!, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegionAllFixedDepositCfg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRegionAllFixedDepositCfg()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.regionId = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.RegionFixedDepositCfg.push(RegionFixedDepositCfg.decode(reader, reader.uint32()))
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): RegionAllFixedDepositCfg {
    return {
      regionId: isSet(object.regionId) ? globalThis.String(object.regionId) : '',
      RegionFixedDepositCfg: globalThis.Array.isArray(object?.RegionFixedDepositCfg)
        ? object.RegionFixedDepositCfg.map((e: any) => RegionFixedDepositCfg.fromJSON(e))
        : [],
    }
  },

  toJSON(message: RegionAllFixedDepositCfg): unknown {
    const obj: any = {}
    if (message.regionId !== '') {
      obj.regionId = message.regionId
    }
    if (message.RegionFixedDepositCfg?.length) {
      obj.RegionFixedDepositCfg = message.RegionFixedDepositCfg.map((e) =>
        RegionFixedDepositCfg.toJSON(e),
      )
    }
    return obj
  },

  create<I extends Exact<DeepPartial<RegionAllFixedDepositCfg>, I>>(
    base?: I,
  ): RegionAllFixedDepositCfg {
    return RegionAllFixedDepositCfg.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<RegionAllFixedDepositCfg>, I>>(
    object: I,
  ): RegionAllFixedDepositCfg {
    const message = createBaseRegionAllFixedDepositCfg()
    message.regionId = object.regionId ?? ''
    message.RegionFixedDepositCfg =
      object.RegionFixedDepositCfg?.map((e) => RegionFixedDepositCfg.fromPartial(e)) || []
    return message
  },
}

function createBaseRegionFixedDepositCfg(): RegionFixedDepositCfg {
  return { term: 0, rate: '', status: 0 }
}

export const RegionFixedDepositCfg: MessageFns<RegionFixedDepositCfg> = {
  encode(message: RegionFixedDepositCfg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.term !== 0) {
      writer.uint32(8).int64(message.term)
    }
    if (message.rate !== '') {
      writer.uint32(18).string(message.rate)
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegionFixedDepositCfg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseRegionFixedDepositCfg()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break
          }

          message.term = longToNumber(reader.int64())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.rate = reader.string()
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.status = reader.int32() as any
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): RegionFixedDepositCfg {
    return {
      term: isSet(object.term) ? globalThis.Number(object.term) : 0,
      rate: isSet(object.rate) ? globalThis.String(object.rate) : '',
      status: isSet(object.status) ? fixedDepositCfgStatusFromJSON(object.status) : 0,
    }
  },

  toJSON(message: RegionFixedDepositCfg): unknown {
    const obj: any = {}
    if (message.term !== 0) {
      obj.term = Math.round(message.term)
    }
    if (message.rate !== '') {
      obj.rate = message.rate
    }
    if (message.status !== 0) {
      obj.status = fixedDepositCfgStatusToJSON(message.status)
    }
    return obj
  },

  create<I extends Exact<DeepPartial<RegionFixedDepositCfg>, I>>(base?: I): RegionFixedDepositCfg {
    return RegionFixedDepositCfg.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<RegionFixedDepositCfg>, I>>(
    object: I,
  ): RegionFixedDepositCfg {
    const message = createBaseRegionFixedDepositCfg()
    message.term = object.term ?? 0
    message.rate = object.rate ?? ''
    message.status = object.status ?? 0
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000)
  const nanos = (date.getTime() % 1_000) * 1_000_000
  return { seconds, nanos }
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000
  millis += (t.nanos || 0) / 1_000_000
  return new globalThis.Date(millis)
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o
  } else if (typeof o === 'string') {
    return new globalThis.Date(o)
  } else {
    return fromTimestamp(Timestamp.fromJSON(o))
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString())
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error('Value is larger than Number.MAX_SAFE_INTEGER')
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error('Value is smaller than Number.MIN_SAFE_INTEGER')
  }
  return num
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
