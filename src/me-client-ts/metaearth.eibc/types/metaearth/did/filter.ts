// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: metaearth/did/filter.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'metaearth.did'

export interface FilterLogger {
  did: string
  sid: string
  filters: Uint8Array[]
}

function createBaseFilterLogger(): FilterLogger {
  return { did: '', sid: '', filters: [] }
}

export const FilterLogger: MessageFns<FilterLogger> = {
  encode(message: FilterLogger, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.did !== '') {
      writer.uint32(10).string(message.did)
    }
    if (message.sid !== '') {
      writer.uint32(18).string(message.sid)
    }
    for (const v of message.filters) {
      writer.uint32(26).bytes(v!)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilterLogger {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseFilterLogger()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.did = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.sid = reader.string()
          continue
        }
        case 3: {
          if (tag !== 26) {
            break
          }

          message.filters.push(reader.bytes())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): FilterLogger {
    return {
      did: isSet(object.did) ? globalThis.String(object.did) : '',
      sid: isSet(object.sid) ? globalThis.String(object.sid) : '',
      filters: globalThis.Array.isArray(object?.filters)
        ? object.filters.map((e: any) => bytesFromBase64(e))
        : [],
    }
  },

  toJSON(message: FilterLogger): unknown {
    const obj: any = {}
    if (message.did !== '') {
      obj.did = message.did
    }
    if (message.sid !== '') {
      obj.sid = message.sid
    }
    if (message.filters?.length) {
      obj.filters = message.filters.map((e) => base64FromBytes(e))
    }
    return obj
  },

  create<I extends Exact<DeepPartial<FilterLogger>, I>>(base?: I): FilterLogger {
    return FilterLogger.fromPartial(base ?? ({} as any))
  },
  fromPartial<I extends Exact<DeepPartial<FilterLogger>, I>>(object: I): FilterLogger {
    const message = createBaseFilterLogger()
    message.did = object.did ?? ''
    message.sid = object.sid ?? ''
    message.filters = object.filters?.map((e) => e) || []
    return message
  },
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, 'base64'))
  } else {
    const bin = globalThis.atob(b64)
    const arr = new Uint8Array(bin.length)
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i)
    }
    return arr
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString('base64')
  } else {
    const bin: string[] = []
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte))
    })
    return globalThis.btoa(bin.join(''))
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

type KeysOfUnion<T> = T extends T ? keyof T : never
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never }

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T
}
