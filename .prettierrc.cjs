module.exports = {
  // Print width max 100 characters
  printWidth: 100,
  // Tab width 2 spaces
  tabWidth: 2,
  // Indent with spaces instead of tabs
  useTabs: false,
  // No semicolons at the end of statements
  semi: false,
  // Use single quotes
  singleQuote: true,
  // Quote object properties only when required
  quoteProps: 'as-needed',
  // Use double quotes in JSX
  jsxSingleQuote: false,
  // Trailing commas
  trailingComma: 'all',
  // Print spaces between brackets in object literals
  bracketSpacing: true,
  // Put the > of a multi-line JSX element on a new line
  jsxBracketSameLine: false,
  // Include parentheses around a sole arrow function parameter
  arrowParens: 'always',
  // Format the entire contents of the file
  rangeStart: 0,
  rangeEnd: Infinity,
  // Do not require a special @prettier pragma
  requirePragma: false,
  // Do not automatically insert a @prettier pragma
  insertPragma: false,
  // Prose wrap behavior
  proseWrap: 'preserve',
  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',
  // End of line (lf)
  endOfLine: 'lf',
}
